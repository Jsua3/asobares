# Panel administrativo F1–F3 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Levantar la capa visual compartida del panel (tokens, tema propio, cuatro componentes), sembrar datos con forma temporal, y reemplazar el tablero de fábrica por un tablero por rol con cola de pendientes accionable.

**Architecture:** Los tokens semánticos que hoy viven solo en `resources/css/app.css` se extraen a `resources/css/tokens.css` y se importan tanto desde el sitio como desde un tema nuevo de Filament (`resources/css/filament/admin/theme.css`). Sobre esa base se construyen cuatro componentes Blade (`vidrio`, `kpi`, `cola`, `grafica`) que consumen los tokens. El tablero pasa a ser una página propia que hereda de `Filament\Pages\Dashboard`, y la cola de pendientes se resuelve preguntando a la policy (`publicar`) por cada modelo publicable, igual que ya hace `FlujoDeAprobacionObserver`.

**Tech Stack:** Laravel 13.23, Filament 4.12, Livewire 3.8, Tailwind CSS v4, Alpine.js, Chart.js (empaquetado por Filament), PHPUnit 12, PHP 8.5.

## Global Constraints

- **Identificadores en español. Decisión tomada, no la reabras.** `CLAUDE.md` dice «Código en inglés, UI en español» y, dos líneas más abajo, «follow all existing code conventions... check sibling files for the correct structure, approach, and naming». Se contradice. El código existente es íntegramente español (`ResumenDelGremio`, `EsPublicable`, `avisarAQuienAprueba`, `PoliticaDeContenido`, `FlujoDeAprobacionObserver`), y **el dueño del proyecto resolvió el 5 ago 2026 que gobierna la convención existente**. Los identificadores nuevos van en español: `ColaDePendientes`, `PendientesDeAprobacion`, `RecaudoMensual`, `ConsultaGuia`. Mezclar idiomas ahora es peor que cualquiera de los dos puros. **No es un defecto y no debe reportarse como tal.**
- **PSR-12 vía Pint.** Ejecutar `vendor/bin/pint --dirty --format agent` antes de cada commit.
- **PHPUnit, nunca Pest.** Crear tests con `php artisan make:test --phpunit {Nombre}`.
- **Cero colores cableados en vistas.** Solo tokens (`bg-superficie`, `text-tinta`, `border-linea`, `text-acento`…). La prueba de guardia `TemaClaroOscuroTest::test_ninguna_vista_publica_conserva_clases_de_tema_cableadas` lo verifica y en la Tarea 6 se extiende al panel.
- **Ambos temas al mismo nivel.** Todo lo que se construya debe verse correcto en claro y en oscuro. Contraste AA en los dos.
- **Movimiento siempre bajo `@media (prefers-reduced-motion: reduce)`.**
- **`php artisan view:clear` antes de `npm run build`.** `app.css` escanea `storage/framework/views/*.php`; sin limpiar, el tamaño del bundle depende de qué vistas estén en caché (se vio pasar de 90 kB a 69 kB solo con eso).
- **Valores exactos de marca:** Pub Black `#0B090A`, Pub Red `#EE4137`, Ambient White `#F5F3F4`. El token `acento` vale `#B71F18` en claro y `#F27166` en oscuro — el Pub Red puro no alcanza AA **como texto** sobre fondo claro.
- **Ejecutar tests con:** `php artisan test --compact --filter=NombreDelTest`.

## Nota de refinamiento sobre el spec

El spec (§1) dice que los `--primary-*` de Filament «se mapean a los tokens». Al leer la documentación de Filament 4 apareció una razón para no hacerlo: Filament 4 tiene `ColorMaps` (`ComponentColorMap`, `ButtonComponentColorMap`, `IconButtonComponentColorMap`) que **eligen el tono por componente y por modo** precisamente para resolver contraste. Sobreescribir `--primary-*` a mano pelearía con ese sistema.

**Decisión de este plan:** se conserva `Color::hex('#EE4137')` en el provider para los componentes propios de Filament (botones, insignias, enlaces), y los tokens `--asb-*` se usan **en nuestros componentes y páginas**. El tema importa los tokens para que estén disponibles; no reemplaza el sistema de color de Filament. Es el espíritu de §1 («no se reemplaza su sistema») con menos riesgo.

## File Structure

| Archivo | Responsabilidad |
|---|---|
| `resources/css/tokens.css` | **Nuevo.** Única fuente de color: `@custom-variant dark`, paletas `@theme`, `:root`, `.dark`, `@theme inline`. Importado por el sitio y por el panel. |
| `resources/css/app.css` | **Modificar.** Pierde los tokens (pasa a importarlos); conserva `@source`, `@layer base`, `@layer components` del sitio. |
| `resources/css/filament/admin/theme.css` | **Nuevo (generado).** Tema del panel: importa tokens, declara `@source` de las vistas del panel, y las utilidades de vidrio. |
| `vite.config.js` | **Modificar.** Añade el tema y `resources/js/panel-graficas.js` al `input`. |
| `app/Providers/Filament/AdminPanelProvider.php` | **Modificar.** `->viteTheme()`, registro del asset de gráficas, y `pages([])` al pasar el tablero a página propia. |
| `resources/js/panel-graficas.js` | **Nuevo.** Plugin global de Chart.js que aplica los tokens a ticks, rejilla y leyenda, y repinta al cambiar de tema. |
| `resources/views/components/panel/vidrio.blade.php` | **Nuevo.** Contenedor de vidrio, dos recetas por tema. |
| `resources/views/components/panel/kpi.blade.php` | **Nuevo.** Tarjeta KPI: valor con conteo animado, delta, enlace. |
| `resources/views/components/panel/cola.blade.php` | **Nuevo.** Fila de pendiente con acción directa. |
| `app/Panel/ColaDePendientes.php` | **Nuevo.** Servicio: qué tiene pendiente de aprobar este usuario, preguntando a la policy. |
| `app/Filament/Pages/Dashboard.php` | **Nuevo.** Tablero propio que reemplaza el de fábrica. |
| `app/Filament/Widgets/PendientesDeAprobacion.php` | **Nuevo.** Banda 1: la cola. |
| `app/Filament/Widgets/ResumenDelGremio.php` | **Modificar.** De 6 tarjetas fijas a 4 por rol, cada una enlazada. |
| `app/Filament/Widgets/InscripcionesDelMes.php` | **Eliminar**, reemplazado por `RecaudoMensual.php`. |
| `app/Filament/Widgets/RecaudoMensual.php` | **Nuevo.** Serie del año en curso, agregada en SQL. |
| `app/Filament/Widgets/AsociadosPorMunicipio.php` | **Modificar.** Quita el `#EE4137` cableado. |
| `database/migrations/2026_08_05_HHMMSS_create_consultas_guia_table.php` | **Nuevo.** Conteo anónimo de consultas a la guía. |
| `app/Models/ConsultaGuia.php` | **Nuevo.** |
| `database/factories/ConsultaGuiaFactory.php` | **Nuevo.** |
| `app/Http/Controllers/Publico/GuiaController.php` | **Modificar.** Registra la consulta en `index` y en `descargarFormato`. |
| `database/seeders/ConsultaGuiaSeeder.php` | **Nuevo.** |
| `database/seeders/TransaccionSeeder.php` | **Modificar.** 18 meses con estacionalidad. |
| `tests/Feature/Panel/*` | **Nuevos.** Un archivo por tarea con test. |

---

# F1 — Tokens, tema y componentes

## Task 1: Extraer los tokens a un archivo compartido

**Files:**
- Create: `resources/css/tokens.css`
- Modify: `resources/css/app.css`
- Test: `tests/Feature/TemaClaroOscuroTest.php` (ya existe; solo se ejecuta)

**Interfaces:**
- Consumes: nada.
- Produces: `resources/css/tokens.css` con las custom properties `--asb-*` (`fondo`, `superficie`, `superficie-alta`, `fuerte`, `tinta`, `suave`, `tenue`, `apagado`, `linea`, `linea-fuerte`, `acento`, `acento-fuerte`, `marca-panel`, `exito`, `exito-suave`, `exito-fondo`, `exito-linea`, `aviso`, `aviso-suave`, `aviso-fondo`, `aviso-linea`, `resplandor`, `trama`, `sombra-tarjeta`), las paletas `--color-marca-{50..950}`, `--color-noche-{50..950}`, `--color-vino`, `--color-purpura`, `--color-rosa`, el `@custom-variant dark` y el bloque `@theme inline` que expone `--color-fondo`, `--color-superficie`, etc.

Este refactor **es un no-op visual**: se mueven declaraciones, no valores. Es lo que permite que el panel use exactamente los mismos colores que el sitio.

- [ ] **Step 1: Crear `resources/css/tokens.css`**

Mover **literalmente** desde `resources/css/app.css` las líneas 14–175 (el `@custom-variant dark`, el bloque `@theme` con las paletas, `:root`, `.dark` y `@theme inline`), con este encabezado:

```css
/*
 * Única fuente de color de ASOBARES Quindío: la importan el sitio público
 * (`app.css`) y el panel (`filament/admin/theme.css`), de modo que un cambio
 * de paleta se hace en un solo sitio y los dos viajan juntos.
 *
 * Valores tomados del Manual de Marca de Asobares Colombia (Quida Studio):
 * paleta principal Pub Red #EE4137 / Pub Black #0B090A / Ambient White #F5F3F4,
 * paleta secundaria Pub Grey / Wine / Ambient Purple / Ambient Rose, y Poppins
 * como tipografía principal.
 */

/*
 * El tema lo manda la clase `.dark` en <html>, no `prefers-color-scheme`:
 * así el visitante puede forzar claro u oscuro por encima de su sistema
 * operativo. Es la misma convención que usa el panel Filament, de modo que
 * ambos comparten la clave `localStorage.theme` y viajan siempre juntos.
 */
@custom-variant dark (&:where(.dark, .dark *));
```

…seguido del resto tal cual está hoy (paletas `@theme`, `:root`, `.dark`, `@theme inline`). **No cambiar ningún valor hexadecimal ni ninguna opacidad.**

- [ ] **Step 2: Dejar `app.css` importando los tokens**

`resources/css/app.css` queda así en su cabecera (los `@import` van primero por regla de CSS; los `@source` y los `@layer` del sitio se conservan intactos debajo):

```css
@import 'tailwindcss';
@import './tokens.css';

@source '../views/**/*.blade.php';
@source '../../app/**/*.php';
@source '../../vendor/laravel/framework/src/Illuminate/Pagination/resources/views/*.blade.php';
@source '../../storage/framework/views/*.php';
```

Borrar de `app.css` las líneas que se movieron (el `@custom-variant`, el `@theme` de paletas, `:root`, `.dark` y `@theme inline`). **Conservar** `@layer base` y `@layer components` completos.

- [ ] **Step 3: Compilar y verificar que el sitio no cambió**

```bash
php artisan view:clear && npm run build
```

Esperado: build sin errores. El CSS resultante debe seguir conteniendo `--asb-fondo` y `.dark`.

```bash
grep -c "asb-fondo" public/build/assets/app-*.css
```

Esperado: un número mayor que 0.

- [ ] **Step 4: Ejecutar la guardia de tema**

```bash
php artisan test --compact --filter=TemaClaroOscuroTest
```

Esperado: PASS, todos los tests. Si falla `test_ninguna_vista_publica_conserva_clases_de_tema_cableadas` es que se movió algo mal — los tokens no deben haber cambiado de valor.

- [ ] **Step 5: Commit**

```bash
vendor/bin/pint --dirty --format agent
git add resources/css/tokens.css resources/css/app.css
git commit -m "Extrae los tokens de color a un archivo compartido

El panel va a necesitar exactamente los mismos colores que el sitio, y
tenerlos declarados solo en app.css obligaria a duplicarlos. Refactor
no-op: se mueven declaraciones, ningun valor cambia."
```

---

## Task 2: Generar el tema del panel y darle los tokens y Poppins

**Files:**
- Create: `resources/css/filament/admin/theme.css` (generado por Artisan, luego editado)
- Modify: `vite.config.js`
- Modify: `app/Providers/Filament/AdminPanelProvider.php`
- Test: `tests/Feature/Panel/TemaDelPanelTest.php`

**Interfaces:**
- Consumes: `resources/css/tokens.css` de la Tarea 1.
- Produces: tema compilado y registrado con `->viteTheme('resources/css/filament/admin/theme.css')`. Todas las tareas siguientes pueden usar utilidades de token (`bg-superficie`, `text-tinta`, `border-linea`, `text-acento`) dentro de vistas del panel.

- [ ] **Step 1: Generar el tema**

```bash
php artisan make:filament-theme admin --pm=npm --no-interaction
```

Esto crea `resources/css/filament/admin/theme.css`, intenta añadirlo al `input` de `vite.config.js` y registrar `->viteTheme()` en el provider. **Verificar los tres** y completar a mano lo que no haya hecho.

- [ ] **Step 2: Añadir los tokens y las fuentes de escaneo al tema**

Editar `resources/css/filament/admin/theme.css`. **Conservar en la primera línea el `@import` que generó el comando** (Filament 4 importa su propio preset, que a su vez trae Tailwind) y añadir debajo:

```css
@import '../../tokens.css';

@source '../../../../app/Filament/**/*';
@source '../../../../resources/views/filament/**/*';
@source '../../../../resources/views/components/panel/**/*';

@layer base {
    /* Poppins es la tipografía del manual de marca; Filament trae Inter. */
    :root {
        --font-family: 'Poppins', ui-sans-serif, system-ui, sans-serif;
    }

    h1, h2, h3, h4 {
        font-family: var(--font-display);
        font-weight: 700;
        letter-spacing: -0.02em;
    }
}
```

- [ ] **Step 3: Confirmar el registro en `vite.config.js` y en el provider**

`vite.config.js` — el array `input` debe quedar:

```js
input: [
    'resources/css/app.css',
    'resources/js/app.js',
    'resources/css/filament/admin/theme.css',
],
```

`AdminPanelProvider::panel()` — añadir la llamada inmediatamente después de `->brandLogoHeight('2rem')`:

```php
->viteTheme('resources/css/filament/admin/theme.css')
```

- [ ] **Step 4: Escribir el test que falla**

```bash
php artisan make:test --phpunit Panel/TemaDelPanelTest
```

Contenido de `tests/Feature/Panel/TemaDelPanelTest.php`:

```php
<?php

namespace Tests\Feature\Panel;

use App\Providers\Filament\AdminPanelProvider;
use Filament\Panel;
use Illuminate\Support\Facades\File;
use Tests\TestCase;

/**
 * El panel dejó de usar el tema de fábrica: comparte los tokens del sitio,
 * así que un cambio de paleta los mueve a los dos a la vez.
 */
class TemaDelPanelTest extends TestCase
{
    public function test_el_panel_registra_su_tema_compilado(): void
    {
        $panel = (new AdminPanelProvider($this->app))->panel(Panel::make());

        $this->assertSame(
            'resources/css/filament/admin/theme.css',
            $panel->getViteTheme(),
            'El panel debe compilar su propio tema, no usar el de fábrica.'
        );
    }

    public function test_el_tema_del_panel_importa_los_tokens_compartidos(): void
    {
        $tema = File::get(resource_path('css/filament/admin/theme.css'));

        $this->assertStringContainsString(
            "@import '../../tokens.css'",
            $tema,
            'Sin los tokens compartidos el panel repetiría los colores a mano.'
        );
    }

    public function test_los_tokens_viven_en_un_solo_archivo(): void
    {
        $tokens = File::get(resource_path('css/tokens.css'));
        $sitio = File::get(resource_path('css/app.css'));

        // La definición está en tokens.css...
        $this->assertStringContainsString('--asb-fondo: #f5f3f4', $tokens);
        $this->assertStringContainsString('--asb-acento: #b71f18', $tokens);
        $this->assertStringContainsString('--asb-acento: #f27166', $tokens);

        // ...y el sitio la importa en vez de repetirla.
        $this->assertStringContainsString("@import './tokens.css'", $sitio);
        $this->assertStringNotContainsString('--asb-fondo:', $sitio);
    }

    public function test_el_panel_usa_poppins_y_no_la_tipografia_de_fabrica(): void
    {
        $tema = File::get(resource_path('css/filament/admin/theme.css'));

        $this->assertStringContainsString("--font-family: 'Poppins'", $tema);
    }
}
```

- [ ] **Step 5: Ejecutar el test para verificar que falla**

```bash
php artisan test --compact --filter=TemaDelPanelTest
```

Esperado: FAIL antes de aplicar los pasos 2–3 (por `getViteTheme()` nulo o por el `@import` ausente). Si ya se aplicaron, debe pasar.

- [ ] **Step 6: Compilar y ejecutar**

```bash
php artisan view:clear && npm run build && php artisan test --compact --filter=TemaDelPanelTest
```

Esperado: build sin errores y PASS.

- [ ] **Step 7: Commit**

```bash
vendor/bin/pint --dirty --format agent
git add resources/css/filament vite.config.js app/Providers/Filament/AdminPanelProvider.php tests/Feature/Panel/TemaDelPanelTest.php
git commit -m "Da al panel su propio tema con los tokens del sitio

El panel corria con el tema de fabrica, asi que no habia donde escribir
la capa visual de la demo y sus graficas repetian colores a mano.
Ahora comparte tokens.css con el sitio y usa Poppins, la tipografia
del manual de marca."
```

---

## Task 3: Utilidades de vidrio y componente `<x-panel.vidrio>`

**Files:**
- Modify: `resources/css/filament/admin/theme.css`
- Create: `resources/views/components/panel/vidrio.blade.php`
- Test: `tests/Feature/Panel/ComponentesDelPanelTest.php`

**Interfaces:**
- Consumes: tokens de la Tarea 1, tema de la Tarea 2.
- Produces:
  - Clase CSS `.vidrio` (dos recetas por tema) y `.resplandor-panel`.
  - Componente Blade `<x-panel.vidrio>` que acepta el atributo `class` y cualquier otro atributo HTML, y renderiza sus hijos dentro de un `<div class="vidrio …">`.

**El punto de este componente:** en oscuro el vidrio se hace con **luz** (velo blanco translúcido + desenfoque); en claro esa receta desaparece y hay que hacerlo con **sombra y borde**. Copiar la receta oscura es lo que produce el efecto lavado.

- [ ] **Step 1: Añadir los tokens y utilidades de vidrio al tema**

Al final de `resources/css/filament/admin/theme.css`:

```css
/*
 * Vidrio: dos recetas, no una opacidad compartida.
 *
 * En oscuro el efecto lo hace la LUZ (velo blanco translúcido sobre un fondo
 * casi negro). En claro no hay luz que velar, así que el mismo velo se ve
 * lavado y sin borde: allí el efecto lo hacen la SOMBRA y el borde definido.
 *
 * Regla dura del diseño: el vidrio va en el cromo (tarjetas KPI, cabeceras,
 * modales), nunca sobre una tabla de datos. Este proyecto ya arregló dos
 * fallos de contraste (1,22:1 y 2,63:1) y no se reabren.
 */
:root {
    --asb-vidrio-fondo: rgb(255 255 255 / 0.7);
    --asb-vidrio-borde: rgb(11 9 10 / 0.08);
    --asb-vidrio-desenfoque: blur(12px);
    --asb-vidrio-sombra:
        0 1px 2px rgb(11 9 10 / 0.05),
        0 8px 24px rgb(11 9 10 / 0.06);
}

.dark {
    --asb-vidrio-fondo: rgb(255 255 255 / 0.05);
    --asb-vidrio-borde: rgb(255 255 255 / 0.09);
    --asb-vidrio-desenfoque: blur(12px);
    --asb-vidrio-sombra: none;
}

@layer components {
    .vidrio {
        background-color: var(--asb-vidrio-fondo);
        border: 1px solid var(--asb-vidrio-borde);
        border-radius: 1rem;
        box-shadow: var(--asb-vidrio-sombra);
        backdrop-filter: var(--asb-vidrio-desenfoque);
    }

    /* Resplandor rojo discreto, el mismo recurso que usan los heros del sitio. */
    .resplandor-panel {
        background-image: radial-gradient(
            70% 60% at 50% 0%,
            rgb(238 65 55 / var(--asb-resplandor)) 0%,
            transparent 70%
        );
    }

    .vidrio-hover {
        transition:
            border-color 200ms ease,
            transform 200ms ease;
    }

    .vidrio-hover:hover {
        border-color: rgb(238 65 55 / 0.45);
        transform: translateY(-2px);
    }
}

/* Sin esto el movimiento es una barrera de accesibilidad, no un detalle. */
@media (prefers-reduced-motion: reduce) {
    .vidrio-hover,
    .vidrio-hover:hover {
        transition: none;
        transform: none;
    }
}
```

- [ ] **Step 2: Crear el componente**

`resources/views/components/panel/vidrio.blade.php`:

```blade
@props(['resplandor' => false, 'hover' => false])

{{--
    Contenedor de vidrio del panel. Las dos recetas (luz en oscuro, sombra en
    claro) viven en la clase `.vidrio` del tema, no aquí: así el componente no
    conoce colores y un cambio de paleta no lo toca.
--}}
<div {{ $attributes->class([
    'vidrio',
    'relative overflow-hidden',
    'resplandor-panel' => $resplandor,
    'vidrio-hover' => $hover,
]) }}>
    {{ $slot }}
</div>
```

- [ ] **Step 3: Escribir el test que falla**

```bash
php artisan make:test --phpunit Panel/ComponentesDelPanelTest
```

`tests/Feature/Panel/ComponentesDelPanelTest.php`:

```php
<?php

namespace Tests\Feature\Panel;

use Illuminate\Support\Facades\Blade;
use Illuminate\Support\Facades\File;
use Tests\TestCase;

/**
 * Los cuatro componentes del panel son la capa que levanta las cuatro
 * pantallas estrella. Se prueban por render, no por captura de pantalla.
 */
class ComponentesDelPanelTest extends TestCase
{
    public function test_el_vidrio_rinde_con_su_clase_y_pasa_atributos(): void
    {
        $html = Blade::render('<x-panel.vidrio class="p-4" data-prueba="si">contenido</x-panel.vidrio>');

        $this->assertStringContainsString('vidrio', $html);
        $this->assertStringContainsString('p-4', $html);
        $this->assertStringContainsString('data-prueba="si"', $html);
        $this->assertStringContainsString('contenido', $html);
    }

    public function test_el_vidrio_solo_anade_resplandor_y_hover_si_se_piden(): void
    {
        $simple = Blade::render('<x-panel.vidrio>x</x-panel.vidrio>');
        $this->assertStringNotContainsString('resplandor-panel', $simple);
        $this->assertStringNotContainsString('vidrio-hover', $simple);

        $adornado = Blade::render('<x-panel.vidrio resplandor hover>x</x-panel.vidrio>');
        $this->assertStringContainsString('resplandor-panel', $adornado);
        $this->assertStringContainsString('vidrio-hover', $adornado);
    }

    /**
     * El vidrio con la receta de oscuro sobre fondo claro se ve lavado: por eso
     * hay dos juegos de valores y no un `opacity` compartido.
     */
    public function test_el_vidrio_tiene_receta_distinta_en_cada_tema(): void
    {
        $tema = File::get(resource_path('css/filament/admin/theme.css'));

        // Claro: velo casi opaco + sombra que despega la tarjeta.
        $this->assertStringContainsString('--asb-vidrio-fondo: rgb(255 255 255 / 0.7)', $tema);
        $this->assertStringContainsString('--asb-vidrio-sombra:', $tema);

        // Oscuro: velo tenue y sin sombra, el contraste lo da la superficie.
        $this->assertStringContainsString('--asb-vidrio-fondo: rgb(255 255 255 / 0.05)', $tema);
        $this->assertStringContainsString('--asb-vidrio-sombra: none', $tema);
    }

    public function test_el_movimiento_respeta_prefers_reduced_motion(): void
    {
        $tema = File::get(resource_path('css/filament/admin/theme.css'));

        $this->assertStringContainsString('prefers-reduced-motion: reduce', $tema);
    }
}
```

- [ ] **Step 4: Ejecutar para verificar que falla**

```bash
php artisan test --compact --filter=ComponentesDelPanelTest
```

Esperado: FAIL con «View [components.panel.vidrio] not found» si aún no se creó el componente.

- [ ] **Step 5: Ejecutar hasta verde**

```bash
php artisan test --compact --filter=ComponentesDelPanelTest
```

Esperado: PASS, 4 tests.

- [ ] **Step 6: Commit**

```bash
vendor/bin/pint --dirty --format agent
git add resources/css/filament/admin/theme.css resources/views/components/panel/vidrio.blade.php tests/Feature/Panel/ComponentesDelPanelTest.php
git commit -m "Anade la capa de vidrio del panel con receta por tema

En oscuro el efecto lo hace la luz; en claro, la sombra y el borde.
Copiar la receta oscura al tema claro es lo que deja las tarjetas
lavadas, asi que son dos juegos de valores y no una opacidad."
```

---

## Task 4: Componente `<x-panel.kpi>`

**Files:**
- Create: `resources/views/components/panel/kpi.blade.php`
- Modify: `tests/Feature/Panel/ComponentesDelPanelTest.php`

**Interfaces:**
- Consumes: `<x-panel.vidrio>` de la Tarea 3.
- Produces: componente `<x-panel.kpi>` con esta firma exacta:

```
etiqueta: string          — el nombre del indicador (obligatorio)
valor: string|int         — el número ya formateado (obligatorio)
url: ?string = null       — a dónde lleva el clic; sin url no es enlace
delta: ?float = null      — variación porcentual contra el periodo anterior
detalle: ?string = null   — línea de contexto bajo el valor
icono: ?string = null     — nombre de icono heroicon, p. ej. 'heroicon-o-banknotes'
n: ?int = null            — tamaño de muestra; si viene, se rotula
```

**El principio que codifica:** un KPI que no es enlace es un número muerto. Por eso `url` produce un `<a>` envolvente y el componente rotula «Ver detalle».

- [ ] **Step 1: Escribir los tests que fallan**

Añadir a `tests/Feature/Panel/ComponentesDelPanelTest.php`, dentro de la clase:

```php
    public function test_el_kpi_muestra_etiqueta_valor_y_detalle(): void
    {
        $html = Blade::render(
            '<x-panel.kpi etiqueta="Recaudado este mes" valor="$1.250.000" detalle="14 transacciones" />'
        );

        $this->assertStringContainsString('Recaudado este mes', $html);
        $this->assertStringContainsString('$1.250.000', $html);
        $this->assertStringContainsString('14 transacciones', $html);
    }

    /** Un KPI que no lleva a ningún lado es un número muerto. */
    public function test_el_kpi_con_url_es_un_enlace_y_sin_url_no(): void
    {
        $conEnlace = Blade::render(
            '<x-panel.kpi etiqueta="Mensajes" valor="7" url="/admin/mensajes" />'
        );
        $this->assertStringContainsString('href="/admin/mensajes"', $conEnlace);
        $this->assertStringContainsString('Ver detalle', $conEnlace);

        $sinEnlace = Blade::render('<x-panel.kpi etiqueta="Mensajes" valor="7" />');
        $this->assertStringNotContainsString('<a ', $sinEnlace);
    }

    /**
     * ⚠️ No se puede afirmar sobre el nombre del icono: `x-filament::icon`
     * renderiza un SVG crudo (`<svg class="fi-icon fi-size-md …">`) donde el
     * nombre `heroicon-o-arrow-trending-up` no aparece por ningún lado.
     *
     * Se afirma sobre lo que el usuario percibe de verdad: el signo en el
     * texto —que es lo que hace legible la dirección sin distinguir colores,
     * WCAG 1.4.1— y el token de color que la refuerza.
     */
    public function test_el_kpi_marca_la_direccion_del_delta(): void
    {
        $sube = Blade::render('<x-panel.kpi etiqueta="Recaudo" valor="10" :delta="12.5" />');
        $this->assertStringContainsString('+12,5', $sube);
        $this->assertStringContainsString('text-exito', $sube);

        $baja = Blade::render('<x-panel.kpi etiqueta="Recaudo" valor="10" :delta="-8.0" />');
        $this->assertStringContainsString('−8,0', $baja);
        $this->assertStringContainsString('text-acento', $baja);
    }

    /**
     * Ninguna cifra se presenta sin su n. Un gremio que lleva porcentajes sin
     * tamaño de muestra a una alcaldía pierde credibilidad una sola vez.
     */
    public function test_el_kpi_rotula_el_tamano_de_muestra_cuando_se_le_da(): void
    {
        $html = Blade::render('<x-panel.kpi etiqueta="Mora" valor="18 %" :n="11" />');

        $this->assertStringContainsString('n = 11', $html);
        $this->assertStringContainsString('muestra pequeña', $html);
    }

    public function test_el_kpi_no_usa_colores_cableados(): void
    {
        $fuente = File::get(resource_path('views/components/panel/kpi.blade.php'));

        foreach (\Tests\Feature\TemaClaroOscuroTest::clasesProhibidas() as $patron => $motivo) {
            $this->assertSame(
                0,
                preg_match($patron, $fuente),
                "El KPI tiene una clase de tema cableada: {$motivo}"
            );
        }
    }
```

- [ ] **Step 2: Ejecutar para verificar que fallan**

```bash
php artisan test --compact --filter=ComponentesDelPanelTest
```

Esperado: FAIL con «View [components.panel.kpi] not found».

- [ ] **Step 3: Crear el componente**

`resources/views/components/panel/kpi.blade.php`:

```blade
@props([
    'etiqueta',
    'valor',
    'url' => null,
    'delta' => null,
    'detalle' => null,
    'icono' => null,
    'n' => null,
])

@php
    // Treinta es la regla de oro convencional: por debajo de esa muestra una
    // cifra no sostiene una afirmación. Se rotula para no presentar ruido
    // como tendencia — con 60 asociados, casi todas las series del gremio
    // caen aquí en 2026, y la interfaz tiene que decirlo.
    $muestraChica = $n !== null && $n < 30;
    $sube = $delta !== null && $delta >= 0;
    $deltaTexto = $delta === null
        ? null
        : ($sube ? '+' : '−').number_format(abs((float) $delta), 1, ',', '.').' %';
@endphp

<x-panel.vidrio :hover="filled($url)" class="p-5">
    <div class="flex items-start justify-between gap-3">
        <p class="antetitulo text-tenue">{{ $etiqueta }}</p>

        @if ($icono)
            <x-filament::icon :icon="$icono" class="h-5 w-5 text-acento" />
        @endif
    </div>

    {{--
        El conteo animado sube desde cero al entrar en pantalla. `x-intersect`
        evita animar tarjetas que nadie está mirando, y quien pidió menos
        movimiento ve el valor final de una.
    --}}
    <p
        class="mt-3 font-display text-3xl font-bold tracking-tight text-fuerte"
        x-data="{
            mostrado: @js((string) $valor),
            animar() {
                const crudo = @js((string) $valor);
                const destino = Number(crudo.replace(/[^0-9,-]/g, '').replace(',', '.'));

                if (! Number.isFinite(destino) || destino === 0) return;
                if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;

                const inicio = performance.now();
                const paso = (ahora) => {
                    const avance = Math.min((ahora - inicio) / 600, 1);
                    const valor = Math.round(destino * (1 - Math.pow(1 - avance, 3)));
                    this.mostrado = crudo.replace(/[0-9.,]+/, valor.toLocaleString('es-CO'));
                    if (avance < 1) requestAnimationFrame(paso);
                    else this.mostrado = crudo;
                };
                requestAnimationFrame(paso);
            },
        }"
        x-intersect.once="animar()"
        x-text="mostrado"
    >{{ $valor }}</p>

    @if ($deltaTexto)
        <p @class([
            'mt-1 flex items-center gap-1 text-sm font-medium',
            'text-exito' => $sube,
            'text-acento' => ! $sube,
        ])>
            <x-filament::icon
                :icon="$sube ? 'heroicon-o-arrow-trending-up' : 'heroicon-o-arrow-trending-down'"
                class="h-4 w-4"
            />
            {{ $deltaTexto }}
            <span class="text-tenue font-normal">vs. mes anterior</span>
        </p>
    @endif

    @if ($detalle)
        <p class="mt-2 text-sm text-tenue">{{ $detalle }}</p>
    @endif

    @if ($n !== null)
        <p @class(['mt-2 text-xs', $muestraChica ? 'text-aviso' : 'text-apagado'])>
            n = {{ $n }}@if ($muestraChica) · muestra pequeña @endif
        </p>
    @endif

    @if ($url)
        <a href="{{ $url }}" class="mt-4 inline-flex items-center gap-1 text-sm font-medium text-acento hover:text-acento-fuerte">
            Ver detalle
            <x-filament::icon icon="heroicon-o-arrow-right" class="h-4 w-4" />
        </a>
    @endif
</x-panel.vidrio>
```

- [ ] **Step 4: Ejecutar hasta verde**

```bash
php artisan test --compact --filter=ComponentesDelPanelTest
```

Esperado: PASS, 9 tests.

- [ ] **Step 5: Commit**

```bash
vendor/bin/pint --dirty --format agent
git add resources/views/components/panel/kpi.blade.php tests/Feature/Panel/ComponentesDelPanelTest.php
git commit -m "Anade la tarjeta KPI del panel

Codifica dos reglas del diseno: un KPI que no es enlace es un numero
muerto, y ninguna cifra se presenta sin su n. El conteo animado se
apaga con prefers-reduced-motion."
```

---

## Task 5: Componente `<x-panel.cola>`

**Files:**
- Create: `resources/views/components/panel/cola.blade.php`
- Modify: `tests/Feature/Panel/ComponentesDelPanelTest.php`

**Interfaces:**
- Consumes: nada de tareas previas (usa solo tokens).
- Produces: componente `<x-panel.cola>` con firma:

```
etiqueta: string        — qué hay pendiente, p. ej. "3 vacantes por aprobar"
url: string             — a dónde va el botón de acción (obligatorio)
icono: string = 'heroicon-o-clock'
antiguedad: ?string = null   — p. ej. "la más antigua, hace 4 días"
urgente: bool = false        — pinta el acento de aviso
accion: string = 'Revisar'   — texto del botón
```

- [ ] **Step 1: Escribir los tests que fallan**

Añadir a `tests/Feature/Panel/ComponentesDelPanelTest.php`:

```php
    public function test_la_cola_muestra_etiqueta_antiguedad_y_accion(): void
    {
        $html = Blade::render(
            '<x-panel.cola etiqueta="3 vacantes por aprobar" url="/admin/vacantes" antiguedad="la más antigua, hace 4 días" />'
        );

        $this->assertStringContainsString('3 vacantes por aprobar', $html);
        $this->assertStringContainsString('la más antigua, hace 4 días', $html);
        $this->assertStringContainsString('href="/admin/vacantes"', $html);
        $this->assertStringContainsString('Revisar', $html);
    }

    public function test_la_cola_permite_renombrar_la_accion(): void
    {
        $html = Blade::render(
            '<x-panel.cola etiqueta="7 mensajes" url="/admin/mensajes" accion="Abrir bandeja" />'
        );

        $this->assertStringContainsString('Abrir bandeja', $html);
        $this->assertStringNotContainsString('Revisar', $html);
    }

    /** Lo urgente no se distingue solo por color (WCAG 1.4.1). */
    public function test_la_cola_marca_lo_urgente_con_algo_mas_que_color(): void
    {
        $html = Blade::render(
            '<x-panel.cola etiqueta="2 PQR vencidos" url="/admin/mensajes" urgente />'
        );

        $this->assertStringContainsString('text-aviso', $html);
        $this->assertStringContainsString('Urgente', $html);
    }

    public function test_la_cola_no_usa_colores_cableados(): void
    {
        $fuente = File::get(resource_path('views/components/panel/cola.blade.php'));

        foreach (\Tests\Feature\TemaClaroOscuroTest::clasesProhibidas() as $patron => $motivo) {
            $this->assertSame(0, preg_match($patron, $fuente), "La cola tiene una clase cableada: {$motivo}");
        }
    }
```

- [ ] **Step 2: Ejecutar para verificar que fallan**

```bash
php artisan test --compact --filter=ComponentesDelPanelTest
```

Esperado: FAIL con «View [components.panel.cola] not found».

- [ ] **Step 3: Crear el componente**

`resources/views/components/panel/cola.blade.php`:

```blade
@props([
    'etiqueta',
    'url',
    'icono' => 'heroicon-o-clock',
    'antiguedad' => null,
    'urgente' => false,
    'accion' => 'Revisar',
])

{{--
    Fila de la cola de pendientes. Lo urgente se distingue por icono y por
    rótulo, no solo por color: el estado activo del selector de tema del sitio
    ya incumplió WCAG 1.4.1 una vez por confiar únicamente en el color.
--}}
<div class="flex items-center gap-4 border-b border-linea py-3 last:border-0">
    <span @class([
        'flex h-9 w-9 shrink-0 items-center justify-center rounded-full',
        'bg-aviso-fondo text-aviso' => $urgente,
        'bg-marca-panel text-acento' => ! $urgente,
    ])>
        <x-filament::icon :icon="$icono" class="h-5 w-5" />
    </span>

    <div class="min-w-0 flex-1">
        <p class="truncate text-sm font-medium text-tinta">
            {{ $etiqueta }}
            @if ($urgente)
                <span class="ml-1 text-xs font-semibold uppercase tracking-wide text-aviso">· Urgente</span>
            @endif
        </p>

        @if ($antiguedad)
            <p class="mt-0.5 text-xs text-tenue">{{ $antiguedad }}</p>
        @endif
    </div>

    <a
        href="{{ $url }}"
        class="shrink-0 rounded-lg border border-linea px-3 py-1.5 text-sm font-medium text-tinta transition-colors hover:border-acento hover:text-acento"
    >
        {{ $accion }}
    </a>
</div>
```

- [ ] **Step 4: Ejecutar hasta verde**

```bash
php artisan test --compact --filter=ComponentesDelPanelTest
```

Esperado: PASS, 13 tests.

- [ ] **Step 5: Commit**

```bash
vendor/bin/pint --dirty --format agent
git add resources/views/components/panel/cola.blade.php tests/Feature/Panel/ComponentesDelPanelTest.php
git commit -m "Anade la fila de cola de pendientes del panel

Lo urgente se marca con icono y rotulo, no solo con color: el selector
de tema del sitio ya incumplio WCAG 1.4.1 por confiar en el color solo."
```

---

## Task 6: Gráficas conscientes del tema y limpieza del `#EE4137` cableado

**Files:**
- Create: `resources/js/panel-graficas.js`
- Modify: `vite.config.js`
- Modify: `app/Providers/Filament/AdminPanelProvider.php`
- Modify: `app/Filament/Widgets/AsociadosPorMunicipio.php`
- Test: `tests/Feature/Panel/GraficasDelPanelTest.php`

**Interfaces:**
- Consumes: tokens de la Tarea 1, tema de la Tarea 2.
- Produces: plugin global de Chart.js registrado como asset de Filament. Todo `ChartWidget` del panel hereda ticks, rejilla y leyenda en el color del tema activo, y se repinta al alternar tema. Los widgets dejan de escribir hexadecimales.

**Precisión importante sobre el problema real:** el relleno de una barra en Pub Red `#EE4137` **funciona en los dos temas** — la restricción AA del token `acento` es para **texto**, no para rellenos. El fallo real de las gráficas actuales es otro: Chart.js pinta **ticks, rejilla y leyenda** en un gris fijo que no sigue el tema y queda con poco contraste en oscuro. Eso es lo que arregla este plugin.

- [ ] **Step 1: Crear el plugin**

`resources/js/panel-graficas.js`:

```js
/*
 * Las gráficas del panel siguen el tema.
 *
 * El relleno de las series se queda en Pub Red: como relleno funciona en los
 * dos temas (la restricción AA del token `acento` es para texto). Lo que no
 * seguía el tema eran los ticks, la rejilla y la leyenda, que Chart.js pinta
 * en un gris fijo con poco contraste sobre el fondo oscuro.
 *
 * El plugin lleva su propio registro de instancias (`start`/`stop`) en vez de
 * leer `Chart.instances`: Filament empaqueta Chart.js como módulo y no expone
 * ese global de forma fiable.
 */

const graficas = new Set()

const leerToken = (nombre) =>
    getComputedStyle(document.documentElement).getPropertyValue(nombre).trim()

const plugin = {
    id: 'asobaresTema',

    start(grafica) {
        graficas.add(grafica)
    },

    stop(grafica) {
        graficas.delete(grafica)
    },

    beforeUpdate(grafica) {
        const tinta = leerToken('--asb-tinta')
        const linea = leerToken('--asb-linea')

        if (! tinta) return

        grafica.options.color = tinta

        const leyenda = grafica.options.plugins?.legend?.labels
        if (leyenda) leyenda.color = tinta

        for (const eje of Object.values(grafica.options.scales ?? {})) {
            if (eje.ticks) eje.ticks.color = tinta
            if (eje.grid) eje.grid.color = linea
            if (eje.border) eje.border.color = linea
        }
    },
}

window.filamentChartJsPlugins ??= []
window.filamentChartJsPlugins.push(plugin)

/*
 * Filament conmuta la clase `dark` en <html>. Chart.js no redibuja por eso
 * solo, así que hay que pedírselo: sin esto la gráfica se queda con el color
 * del tema anterior hasta que algo más la fuerce a repintar.
 */
new MutationObserver(() => {
    graficas.forEach((grafica) => grafica.update('none'))
}).observe(document.documentElement, {
    attributes: true,
    attributeFilter: ['class'],
})
```

- [ ] **Step 2: Añadirlo a Vite y registrarlo como asset**

`vite.config.js` — el `input` queda:

```js
input: [
    'resources/css/app.css',
    'resources/js/app.js',
    'resources/css/filament/admin/theme.css',
    'resources/js/panel-graficas.js',
],
```

En `AdminPanelProvider`, añadir los `use` que falten y registrar el asset dentro de `panel()`, justo antes de `->middleware([`:

```php
->assets([
    Js::make('panel-graficas', Vite::asset('resources/js/panel-graficas.js'))->module(),
])
```

Imports necesarios en la cabecera del archivo:

```php
use Filament\Support\Assets\Js;
use Illuminate\Support\Facades\Vite;
```

- [ ] **Step 3: Quitar el hexadecimal cableado del widget de municipios**

En `app/Filament/Widgets/AsociadosPorMunicipio.php`, reemplazar el bloque `datasets` y `getOptions()` por:

```php
        return [
            'datasets' => [[
                'label' => 'Asociados',
                // Pub Red como relleno funciona en los dos temas; lo que no
                // seguía el tema eran ticks y rejilla, y de eso se encarga el
                // plugin `panel-graficas.js`.
                'backgroundColor' => '#EE4137',
                'borderRadius' => 6,
            ]],
            'labels' => $municipios->pluck('nombre')->all(),
        ];
```

…y dejar `getOptions()` así, **sin colores**:

```php
    protected function getOptions(): array
    {
        return [
            'plugins' => ['legend' => ['display' => false]],
            'scales' => [
                'y' => [
                    'beginAtZero' => true,
                    'ticks' => ['precision' => 0],
                    'grid' => [],
                ],
                'x' => [
                    'ticks' => [],
                    'grid' => ['display' => false],
                ],
            ],
        ];
    }
```

Las claves `ticks` y `grid` deben **existir** aunque vengan vacías: el plugin las rellena, y si no están no tiene dónde escribir.

- [ ] **Step 4: Escribir el test que falla**

```bash
php artisan make:test --phpunit Panel/GraficasDelPanelTest
```

`tests/Feature/Panel/GraficasDelPanelTest.php`:

```php
<?php

namespace Tests\Feature\Panel;

use App\Providers\Filament\AdminPanelProvider;
use Filament\Panel;
use Illuminate\Support\Facades\File;
use Tests\TestCase;

/**
 * Las gráficas del panel dejaron de pintar sus etiquetas en un gris fijo.
 */
class GraficasDelPanelTest extends TestCase
{
    public function test_el_plugin_de_graficas_esta_registrado_en_el_panel(): void
    {
        $panel = (new AdminPanelProvider($this->app))->panel(Panel::make());

        $ids = array_map(
            fn ($asset): string => $asset->getId(),
            $panel->getAssets()
        );

        $this->assertContains('panel-graficas', $ids);
    }

    /**
     * Se afirma sobre el texto del archivo porque el proyecto no tiene banco
     * de pruebas de JavaScript (no hay vitest ni jest en `package.json`), y
     * montarlo para un plugin de 40 líneas no se paga. La verificación real
     * de que el repintado funciona es el Paso 7 de esta tarea, a ojo en el
     * navegador y en los dos temas.
     */
    public function test_el_plugin_lee_los_tokens_y_repinta_al_cambiar_de_tema(): void
    {
        $js = File::get(resource_path('js/panel-graficas.js'));

        // Lee del tema activo en vez de llevar colores dentro.
        $this->assertStringContainsString("leerToken('--asb-tinta')", $js);
        $this->assertStringContainsString("leerToken('--asb-linea')", $js);

        // Chart.js no redibuja solo porque cambie la clase de <html>.
        $this->assertStringContainsString('MutationObserver', $js);
        $this->assertStringContainsString("attributeFilter: ['class']", $js);

        // Lleva su propio registro de instancias en vez de un global de Chart.
        $this->assertStringContainsString('start(grafica)', $js);
        $this->assertStringContainsString('stop(grafica)', $js);
    }

    /**
     * Los widgets no deben escribir colores de texto a mano. El relleno de
     * marca sí se admite: como relleno funciona en los dos temas.
     */
    public function test_los_widgets_no_cablean_colores_de_ejes_ni_rejilla(): void
    {
        $hallazgos = [];

        foreach (File::allFiles(app_path('Filament/Widgets')) as $archivo) {
            $contenido = $archivo->getContents();

            foreach (['borderColor' => '#', 'color' => '#'] as $clave => $_) {
                if (preg_match("/'{$clave}'\s*=>\s*'#/", $contenido) === 1) {
                    $hallazgos[] = $archivo->getFilename()." cablea '{$clave}'";
                }
            }
        }

        $this->assertSame([], $hallazgos, implode("\n", $hallazgos));
    }
}
```

- [ ] **Step 5: Ejecutar para verificar que falla**

```bash
php artisan test --compact --filter=GraficasDelPanelTest
```

Esperado: FAIL en `test_el_plugin_de_graficas_esta_registrado_en_el_panel` y en `test_los_widgets_no_cablean_colores_de_ejes_ni_rejilla` (por el `borderColor => '#EE4137'` de `InscripcionesDelMes`, que se elimina en la Tarea 14).

> **Nota de orden:** `InscripcionesDelMes` todavía cablea `borderColor`. Para que esta tarea cierre en verde, aplicar aquí el mismo tratamiento: quitar `'borderColor' => '#EE4137'` y `'backgroundColor' => 'rgba(238, 65, 55, 0.15)'` del dataset y dejar solo `'fill' => true, 'tension' => 0.3`. El widget se elimina completo en la Tarea 14; esto es para no dejar la guardia roja entre tareas.

- [ ] **Step 6: Compilar y ejecutar hasta verde**

```bash
php artisan view:clear && npm run build && php artisan test --compact --filter=GraficasDelPanelTest
```

Esperado: build sin errores y PASS, 3 tests.

- [ ] **Step 7: Verificar en el navegador**

Levantar el panel, entrar como `direccion@asobaresquindio.test` / `Asobares2026*`, y alternar el tema desde el menú de usuario de Filament. Las etiquetas de los ejes de «Asociados publicados por municipio» deben cambiar de color con el tema, sin recargar.

- [ ] **Step 8: Commit**

```bash
vendor/bin/pint --dirty --format agent
git add resources/js/panel-graficas.js vite.config.js app/Providers/Filament/AdminPanelProvider.php app/Filament/Widgets tests/Feature/Panel/GraficasDelPanelTest.php
git commit -m "Hace que las graficas del panel sigan el tema

Chart.js pintaba ticks, rejilla y leyenda en un gris fijo con poco
contraste sobre el fondo oscuro. El plugin los lee de los tokens y
repinta al alternar tema; el relleno de marca se queda, que como
relleno si funciona en los dos temas."
```

---

## Task 7: Extender la guardia de tema a las vistas del panel

**Files:**
- Modify: `tests/Feature/TemaClaroOscuroTest.php`

**Interfaces:**
- Consumes: `TemaClaroOscuroTest::clasesProhibidas()` (ya existe, es `public static`).
- Produces: `test_ninguna_vista_del_panel_conserva_clases_de_tema_cableadas`, que cubre `resources/views/components/panel` y `resources/views/filament`.

Es la prueba que sostiene todo lo anterior: una clase cableada suelta no lanza ningún error, solo deja texto invisible hasta que alguien se queja.

- [ ] **Step 1: Escribir el test**

Añadir a `tests/Feature/TemaClaroOscuroTest.php`, al final de la clase:

```php
    /**
     * El panel se sumó al refactor bicromático, así que la guardia tiene que
     * cubrirlo: sus vistas se rompen en silencio igual que las del sitio.
     */
    public function test_ninguna_vista_del_panel_conserva_clases_de_tema_cableadas(): void
    {
        $directorios = array_filter([
            resource_path('views/components/panel'),
            resource_path('views/filament'),
        ], File::isDirectory(...));

        $this->assertNotEmpty($directorios, 'No hay vistas de panel que vigilar.');

        $hallazgos = [];

        foreach ($directorios as $directorio) {
            foreach (File::allFiles($directorio) as $archivo) {
                $contenido = $archivo->getContents();
                $ruta = str_replace(base_path().DIRECTORY_SEPARATOR, '', $archivo->getPathname());

                foreach (self::clasesProhibidas() as $patron => $motivo) {
                    if (preg_match_all($patron, $contenido, $coincidencias) > 0) {
                        $hallazgos[] = sprintf(
                            '%s → %s (%s)',
                            $ruta,
                            implode(', ', array_unique($coincidencias[0])),
                            $motivo
                        );
                    }
                }
            }
        }

        $this->assertSame([], $hallazgos, "Quedan clases de tema cableadas en el panel:\n".implode("\n", $hallazgos));
    }
```

- [ ] **Step 2: Ejecutar**

```bash
php artisan test --compact --filter=TemaClaroOscuroTest
```

Esperado: PASS. Si falla, hay una clase cableada en un componente de las Tareas 3–5: corregirla usando tokens, no relajar la guardia.

- [ ] **Step 3: Commit**

```bash
vendor/bin/pint --dirty --format agent
git add tests/Feature/TemaClaroOscuroTest.php
git commit -m "Extiende la guardia de tema a las vistas del panel

El panel ya es bicromatico, y sus vistas se rompen en el mismo silencio
que las del sitio: una clase cableada no lanza error, solo deja texto
que nadie ve hasta que alguien se queja."
```

---

# F2 — Consultas de la guía y semilla con forma

## Task 8: Tabla, modelo y factory de `consultas_guia`

**Files:**
- Create: `database/migrations/2026_08_05_HHMMSS_create_consultas_guia_table.php`
- Create: `app/Models/ConsultaGuia.php`
- Create: `database/factories/ConsultaGuiaFactory.php`
- Test: `tests/Feature/Panel/ConsultasDeGuiaTest.php`

**Interfaces:**
- Consumes: `App\Models\Municipio`, `App\Models\RequisitoApertura` (ya existen).
- Produces:
  - Tabla `consultas_guia` con columnas: `id`, `municipio_id` (FK, cascadeOnDelete), `requisito_apertura_id` (FK nullable, nullOnDelete), `created_at`, `updated_at`.
  - Modelo `App\Models\ConsultaGuia` con `$fillable = ['municipio_id', 'requisito_apertura_id']`, relaciones `municipio()` y `requisito()`, y método estático `registrar(int $municipioId, ?int $requisitoId = null): void`.
  - Factory `ConsultaGuiaFactory`.

**Decisión de privacidad, deliberada:** la tabla **no** guarda IP, agente de usuario ni identificador de sesión. Es un conteo agregado y anónimo, así que no es dato personal y no abre frente de Ley 1581. Está probado explícitamente para que nadie lo «mejore» después añadiendo trazabilidad.

- [ ] **Step 1: Escribir el test que falla**

```bash
php artisan make:test --phpunit Panel/ConsultasDeGuiaTest
```

`tests/Feature/Panel/ConsultasDeGuiaTest.php`:

```php
<?php

namespace Tests\Feature\Panel;

use App\Models\ConsultaGuia;
use App\Models\Municipio;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Support\Facades\Schema;
use Tests\TestCase;

/**
 * El mapa de calor del observatorio necesita saber en qué municipios la gente
 * consulta la guía. Es la señal más valiosa que la página insignia produce.
 */
class ConsultasDeGuiaTest extends TestCase
{
    use RefreshDatabase;

    public function test_registrar_deja_una_fila_con_su_municipio(): void
    {
        $municipio = Municipio::factory()->create();

        ConsultaGuia::registrar($municipio->id);

        $this->assertDatabaseCount('consultas_guia', 1);
        $this->assertDatabaseHas('consultas_guia', [
            'municipio_id' => $municipio->id,
            'requisito_apertura_id' => null,
        ]);
    }

    /**
     * Sin IP, sin agente de usuario, sin sesión: es un conteo agregado, no un
     * rastro de personas. Si alguien añade trazabilidad, esto se pone rojo.
     */
    public function test_la_tabla_no_guarda_ningun_dato_personal(): void
    {
        $columnas = Schema::getColumnListing('consultas_guia');

        $this->assertEqualsCanonicalizing(
            ['id', 'municipio_id', 'requisito_apertura_id', 'created_at', 'updated_at'],
            $columnas,
            'La tabla de consultas es un conteo anónimo: no debe ganar columnas de rastreo.'
        );

        foreach (['ip', 'ip_address', 'user_agent', 'agente', 'session_id', 'user_id'] as $prohibida) {
            $this->assertNotContains($prohibida, $columnas);
        }
    }

    public function test_las_consultas_se_pueden_agrupar_por_municipio(): void
    {
        $armenia = Municipio::factory()->create(['nombre' => 'Armenia']);
        $salento = Municipio::factory()->create(['nombre' => 'Salento']);

        ConsultaGuia::factory()->count(5)->for($armenia)->create();
        ConsultaGuia::factory()->count(2)->for($salento)->create();

        $porMunicipio = ConsultaGuia::query()
            ->selectRaw('municipio_id, count(*) as total')
            ->groupBy('municipio_id')
            ->pluck('total', 'municipio_id');

        $this->assertSame(5, (int) $porMunicipio[$armenia->id]);
        $this->assertSame(2, (int) $porMunicipio[$salento->id]);
    }
}
```

- [ ] **Step 2: Ejecutar para verificar que falla**

```bash
php artisan test --compact --filter=ConsultasDeGuiaTest
```

Esperado: FAIL con «Class "App\Models\ConsultaGuia" not found».

- [ ] **Step 3: Crear migración, modelo y factory**

```bash
php artisan make:migration create_consultas_guia_table --no-interaction
php artisan make:model ConsultaGuia --factory --no-interaction
```

Contenido del `up()`/`down()` de la migración:

```php
    public function up(): void
    {
        Schema::create('consultas_guia', function (Blueprint $table): void {
            $table->id();
            $table->foreignId('municipio_id')->constrained()->cascadeOnDelete();
            // Se llena solo al descargar un formato; visitar la guía no lo trae.
            $table->foreignId('requisito_apertura_id')->nullable()->constrained()->nullOnDelete();
            $table->timestamps();

            // El observatorio agrupa por municipio y por mes.
            $table->index(['municipio_id', 'created_at']);
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('consultas_guia');
    }
```

`app/Models/ConsultaGuia.php`:

```php
<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;
use Illuminate\Database\Eloquent\Relations\BelongsTo;

/**
 * Conteo anónimo de consultas a la guía de apertura, por municipio.
 *
 * Es el insumo del mapa de calor del observatorio: «en qué municipios la
 * gente quiere abrir un negocio» es la señal más útil que la página insignia
 * puede darle al gremio para hablar con una alcaldía.
 *
 * ⚠️ No guarda IP, agente de usuario ni sesión, a propósito: así es un
 * agregado y no un dato personal, y queda fuera del alcance de la Ley 1581.
 * `ConsultasDeGuiaTest::test_la_tabla_no_guarda_ningun_dato_personal` lo
 * vigila.
 */
class ConsultaGuia extends Model
{
    /** @use HasFactory<\Database\Factories\ConsultaGuiaFactory> */
    use HasFactory;

    protected $table = 'consultas_guia';

    protected $fillable = ['municipio_id', 'requisito_apertura_id'];

    public function municipio(): BelongsTo
    {
        return $this->belongsTo(Municipio::class);
    }

    public function requisito(): BelongsTo
    {
        return $this->belongsTo(RequisitoApertura::class, 'requisito_apertura_id');
    }

    /** Punto único de escritura: el controlador no arma el modelo a mano. */
    public static function registrar(int $municipioId, ?int $requisitoId = null): void
    {
        static::create([
            'municipio_id' => $municipioId,
            'requisito_apertura_id' => $requisitoId,
        ]);
    }
}
```

`database/factories/ConsultaGuiaFactory.php`:

```php
<?php

namespace Database\Factories;

use App\Models\Municipio;
use Illuminate\Database\Eloquent\Factories\Factory;

/** @extends Factory<\App\Models\ConsultaGuia> */
class ConsultaGuiaFactory extends Factory
{
    /** @return array<string, mixed> */
    public function definition(): array
    {
        return [
            'municipio_id' => Municipio::factory(),
            'requisito_apertura_id' => null,
            'created_at' => fake()->dateTimeBetween('-18 months', 'now'),
        ];
    }
}
```

- [ ] **Step 4: Ejecutar hasta verde**

```bash
php artisan migrate --no-interaction && php artisan test --compact --filter=ConsultasDeGuiaTest
```

Esperado: PASS, 3 tests.

- [ ] **Step 5: Commit**

```bash
vendor/bin/pint --dirty --format agent
git add database/migrations app/Models/ConsultaGuia.php database/factories/ConsultaGuiaFactory.php tests/Feature/Panel/ConsultasDeGuiaTest.php
git commit -m "Anade el conteo anonimo de consultas a la guia

El mapa de calor del observatorio necesita saber donde la gente quiere
abrir un negocio, y ese dato no se registraba en ningun sitio. La tabla
no guarda IP ni agente ni sesion a proposito: es un agregado, no un
rastro de personas, y hay una prueba que lo vigila."
```

---

## Task 9: Registrar la consulta desde `GuiaController`

**Files:**
- Modify: `app/Http/Controllers/Publico/GuiaController.php`
- Modify: `tests/Feature/Panel/ConsultasDeGuiaTest.php`

**Interfaces:**
- Consumes: `ConsultaGuia::registrar()` de la Tarea 8.
- Produces: `GET /abre-tu-negocio` con municipio resuelto registra una fila; `GET /abre-tu-negocio/formato/{requisito}` registra una fila con `requisito_apertura_id`.

- [ ] **Step 1: Escribir los tests que fallan**

Añadir a `tests/Feature/Panel/ConsultasDeGuiaTest.php`:

```php
    public function test_visitar_la_guia_registra_la_consulta_del_municipio(): void
    {
        $this->seed(\Database\Seeders\DatabaseSeeder::class);

        $municipio = \App\Models\Municipio::whereHas(
            'requisitos',
            fn ($q) => $q->publicado()
        )->firstOrFail();

        $this->get('/abre-tu-negocio?municipio='.$municipio->slug)->assertOk();

        $this->assertDatabaseHas('consultas_guia', [
            'municipio_id' => $municipio->id,
            'requisito_apertura_id' => null,
        ]);
    }

    /** Sin municipio resuelto no hay nada que contar. */
    public function test_no_registra_nada_si_ningun_municipio_tiene_guia(): void
    {
        // Base limpia: no hay requisitos publicados, así que no hay selección.
        $this->get('/abre-tu-negocio')->assertOk();

        $this->assertDatabaseCount('consultas_guia', 0);
    }
```

- [ ] **Step 2: Ejecutar para verificar que falla**

```bash
php artisan test --compact --filter=ConsultasDeGuiaTest
```

Esperado: FAIL en `test_visitar_la_guia_registra_la_consulta_del_municipio` — la tabla queda vacía.

- [ ] **Step 3: Registrar en el controlador**

En `app/Http/Controllers/Publico/GuiaController.php`, añadir el import:

```php
use App\Models\ConsultaGuia;
```

En `index()`, justo antes del `return view(...)`:

```php
        // Conteo anónimo para el observatorio: en qué municipios la gente
        // quiere abrir un negocio. Sin municipio resuelto no hay qué contar.
        if ($seleccionado !== null) {
            ConsultaGuia::registrar($seleccionado->id);
        }
```

En `descargarFormato()`, después de las cuatro guardas `abort_unless` y antes del `$nombre = ...`:

```php
        // Descargar el formato es la señal más fuerte de intención real.
        ConsultaGuia::registrar($requisito->municipio_id, $requisito->id);
```

- [ ] **Step 4: Ejecutar hasta verde**

```bash
php artisan test --compact --filter=ConsultasDeGuiaTest
```

Esperado: PASS, 5 tests.

- [ ] **Step 5: Verificar que no se rompió la guía pública**

```bash
php artisan test --compact --filter=SitioPublicoTest
```

Esperado: PASS.

- [ ] **Step 6: Commit**

```bash
vendor/bin/pint --dirty --format agent
git add app/Http/Controllers/Publico/GuiaController.php tests/Feature/Panel/ConsultasDeGuiaTest.php
git commit -m "Registra las consultas a la guia por municipio

Visitar la guia y descargar un formato dejan una fila anonima. Descargar
es la senal mas fuerte de intencion real, asi que se distingue guardando
el requisito."
```

---

## Task 10: Semilla de consultas de la guía

**Files:**
- Create: `database/seeders/ConsultaGuiaSeeder.php`
- Modify: `database/seeders/DatabaseSeeder.php`
- Test: `tests/Feature/Panel/SemillaConFormaTest.php`

**Interfaces:**
- Consumes: `ConsultaGuia` y su factory (Tarea 8).
- Produces: ~420 filas repartidas en 18 meses, concentradas en Armenia y con cola en los municipios pequeños. `ConsultaGuiaSeeder::run()` es idempotente (sale si ya hay filas).

- [ ] **Step 1: Escribir el test que falla**

```bash
php artisan make:test --phpunit Panel/SemillaConFormaTest
```

`tests/Feature/Panel/SemillaConFormaTest.php`:

```php
<?php

namespace Tests\Feature\Panel;

use App\Models\ConsultaGuia;
use Database\Seeders\DatabaseSeeder;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

/**
 * Las gráficas del tablero y del observatorio se diseñan contra estos datos.
 * Con la semilla plana anterior salían líneas en cero, y una gráfica vacía
 * enseña que el tablero no sirve.
 */
class SemillaConFormaTest extends TestCase
{
    use RefreshDatabase;

    protected function setUp(): void
    {
        parent::setUp();
        $this->seed(DatabaseSeeder::class);
    }

    public function test_hay_consultas_de_guia_suficientes_para_un_mapa_de_calor(): void
    {
        $this->assertGreaterThan(200, ConsultaGuia::count());
    }

    public function test_las_consultas_cubren_al_menos_un_ano_y_medio(): void
    {
        $masAntigua = ConsultaGuia::min('created_at');

        $this->assertLessThan(
            now()->subMonths(15),
            \Illuminate\Support\Carbon::parse($masAntigua),
            'La serie debe abarcar ~18 meses para que el observatorio tenga historia.'
        );
    }

    public function test_armenia_concentra_las_consultas_como_en_la_realidad(): void
    {
        $porMunicipio = ConsultaGuia::query()
            ->join('municipios', 'municipios.id', '=', 'consultas_guia.municipio_id')
            ->selectRaw('municipios.nombre, count(*) as total')
            ->groupBy('municipios.nombre')
            ->orderByDesc('total')
            ->pluck('total', 'nombre');

        $this->assertSame('Armenia', $porMunicipio->keys()->first());
        // Y no es un monocultivo: los municipios pequeños también aparecen.
        $this->assertGreaterThanOrEqual(4, $porMunicipio->count());
    }

    public function test_el_seeder_no_duplica_al_correr_dos_veces(): void
    {
        $antes = ConsultaGuia::count();

        $this->seed(\Database\Seeders\ConsultaGuiaSeeder::class);

        $this->assertSame($antes, ConsultaGuia::count());
    }
}
```

- [ ] **Step 2: Ejecutar para verificar que falla**

```bash
php artisan test --compact --filter=SemillaConFormaTest
```

Esperado: FAIL — no hay consultas sembradas.

- [ ] **Step 3: Crear el seeder**

`database/seeders/ConsultaGuiaSeeder.php`:

```php
<?php

namespace Database\Seeders;

use App\Models\ConsultaGuia;
use App\Models\Municipio;
use Illuminate\Database\Seeder;

/**
 * Consultas a la guía repartidas en 18 meses.
 *
 * La forma importa tanto como el volumen: Armenia concentra —es la capital y
 * donde está el grueso del sector—, los municipios turísticos van detrás, y
 * los pequeños dejan una cola fina. Una distribución plana no se parece a
 * nada y delata la semilla en la primera mirada.
 */
class ConsultaGuiaSeeder extends Seeder
{
    /** Peso relativo de cada municipio en las consultas. */
    private const array PESOS = [
        'Armenia' => 45,
        'Calarcá' => 12,
        'Salento' => 11,
        'Montenegro' => 8,
        'La Tebaida' => 7,
        'Quimbaya' => 6,
        'Circasia' => 6,
        'Filandia' => 5,
    ];

    public function run(): void
    {
        if (ConsultaGuia::count() > 0) {
            return;
        }

        $municipios = Municipio::pluck('id', 'nombre');
        $filas = [];

        foreach (self::PESOS as $nombre => $peso) {
            $municipioId = $municipios[$nombre] ?? null;

            if ($municipioId === null) {
                continue;
            }

            foreach (range(0, 17) as $mesesAtras) {
                // Interés creciente: el mes más reciente pesa ~el doble que el
                // más antiguo, que es como se comporta un sitio que va ganando
                // tráfico en vez de nacer con su máximo.
                $crecimiento = 1 + ((17 - $mesesAtras) / 17);
                $cuantas = (int) round($peso * $crecimiento / 10);

                foreach (range(1, max($cuantas, 1)) as $i) {
                    $fecha = now()
                        ->subMonths($mesesAtras)
                        ->startOfMonth()
                        ->addDays(random_int(0, 27))
                        ->addHours(random_int(8, 22));

                    $filas[] = [
                        'municipio_id' => $municipioId,
                        'requisito_apertura_id' => null,
                        'created_at' => $fecha,
                        'updated_at' => $fecha,
                    ];
                }
            }
        }

        // Inserción en lote: son cientos de filas y no hace falta un modelo
        // por cada una.
        foreach (array_chunk($filas, 200) as $lote) {
            ConsultaGuia::insert($lote);
        }
    }
}
```

- [ ] **Step 4: Registrarlo en `DatabaseSeeder`**

En `database/seeders/DatabaseSeeder.php`, dentro del `$this->call([...])`, añadir tras `RequisitoAperturaSeeder::class`:

```php
            ConsultaGuiaSeeder::class,
```

- [ ] **Step 5: Ejecutar hasta verde**

```bash
php artisan migrate:fresh --seed --no-interaction && php artisan test --compact --filter=SemillaConFormaTest
```

Esperado: PASS, 4 tests.

- [ ] **Step 6: Commit**

```bash
vendor/bin/pint --dirty --format agent
git add database/seeders/ConsultaGuiaSeeder.php database/seeders/DatabaseSeeder.php tests/Feature/Panel/SemillaConFormaTest.php
git commit -m "Siembra 18 meses de consultas a la guia con forma creible

La forma importa tanto como el volumen: Armenia concentra, los
turisticos van detras y los pequenos dejan cola. Una distribucion plana
no se parece a nada y delata la semilla en la primera mirada."
```

---

## Task 11: Transacciones de 18 meses con estacionalidad

**Files:**
- Modify: `database/seeders/TransaccionSeeder.php`
- Modify: `tests/Feature/Panel/SemillaConFormaTest.php`

**Interfaces:**
- Consumes: `Transaccion`, `ConceptoTransaccion`, `EstadoTransaccion`, `MetodoPago`, `CarteraSeeder::MENSUALIDAD` (todos existen).
- Produces: además de las transacciones actuales, ~18 meses de mensualidades aprobadas con estacionalidad (pico en diciembre, valle en enero–febrero). El método privado existente `crear()` se conserva y se le añade el parámetro `?\DateTimeInterface $fecha = null`.

**Por qué:** la banda 3 del tablero grafica recaudo mensual. Sin serie, no hay gráfica.

- [ ] **Step 1: Escribir los tests que fallan**

Añadir a `tests/Feature/Panel/SemillaConFormaTest.php`:

```php
    public function test_hay_recaudo_en_al_menos_doce_meses_distintos(): void
    {
        $meses = \App\Models\Transaccion::query()
            ->where('estado', \App\Enums\EstadoTransaccion::Aprobada)
            ->get()
            ->map(fn ($t): string => $t->created_at->format('Y-m'))
            ->unique();

        $this->assertGreaterThanOrEqual(
            12,
            $meses->count(),
            'Sin serie mensual el gráfico de recaudo es una línea plana.'
        );
    }

    public function test_diciembre_recauda_mas_que_enero(): void
    {
        $porMes = \App\Models\Transaccion::query()
            ->where('estado', \App\Enums\EstadoTransaccion::Aprobada)
            ->get()
            ->groupBy(fn ($t): string => $t->created_at->format('m'))
            ->map(fn ($grupo): float => (float) $grupo->sum('monto'));

        $this->assertArrayHasKey('12', $porMes->all());
        $this->assertArrayHasKey('01', $porMes->all());
        $this->assertGreaterThan(
            $porMes['01'],
            $porMes['12'],
            'La vida nocturna factura en diciembre; la semilla debe parecerlo.'
        );
    }
```

- [ ] **Step 2: Ejecutar para verificar que falla**

```bash
php artisan test --compact --filter=SemillaConFormaTest
```

Esperado: FAIL — hoy todas las transacciones caen en los últimos 25 días.

- [ ] **Step 3: Añadir la serie histórica al seeder**

En `database/seeders/TransaccionSeeder.php`, cambiar la firma de `crear()` para aceptar fecha:

```php
    /** @param  array<string, mixed>  $extra */
    private function crear(
        ConceptoTransaccion $concepto,
        int $monto,
        EstadoTransaccion $estado,
        MetodoPago $metodo,
        array $extra = [],
        ?\DateTimeInterface $fecha = null
    ): Transaccion {
        $cuando = $fecha ?? now()->subDays(random_int(1, 25));

        return Transaccion::create([
            'referencia' => Transaccion::generarReferencia(),
            'concepto' => $concepto,
            'monto' => $monto,
            'moneda' => 'COP',
            'estado' => $estado,
            'metodo' => $metodo,
            'payload' => [
                'origen' => 'semilla',
                'pasarela' => 'fake',
                'registrado_en' => $cuando->format(\DateTimeInterface::ATOM),
            ],
            'created_at' => $cuando,
            'updated_at' => $cuando,
        ] + $extra);
    }
```

Y añadir al final de `run()`, antes del cierre del método:

```php
        $this->sembrarHistorialDeMensualidades();
```

Más este método nuevo:

```php
    /**
     * Dieciocho meses de mensualidades con estacionalidad.
     *
     * El tablero grafica recaudo mensual; con las transacciones apretadas en
     * los últimos 25 días la gráfica era una línea plana, y una gráfica vacía
     * enseña que el tablero no sirve.
     *
     * La forma es la del sector: diciembre factura, enero y febrero son el
     * valle, y la base de afiliados va creciendo mes a mes.
     */
    private function sembrarHistorialDeMensualidades(): void
    {
        /** @var array<int, int> índice de mes (1-12) => factor estacional en centésimas */
        $estacionalidad = [
            1 => 70, 2 => 75, 3 => 90, 4 => 95, 5 => 100, 6 => 105,
            7 => 110, 8 => 105, 9 => 100, 10 => 105, 11 => 115, 12 => 150,
        ];

        $asociados = Asociado::query()->has('cartera')->pluck('id')->all();

        if ($asociados === []) {
            return;
        }

        foreach (range(1, 18) as $mesesAtras) {
            $mes = now()->subMonths($mesesAtras)->startOfMonth();

            // La base crece: hace 18 meses pagaban ~el 40 % de los de hoy.
            $crecimiento = 0.4 + (0.6 * (18 - $mesesAtras) / 18);
            $factor = $estacionalidad[(int) $mes->format('n')] / 100;
            $cuantos = (int) round(count($asociados) * 0.55 * $crecimiento * $factor);
            $cuantos = max(min($cuantos, count($asociados)), 1);

            $pagadores = array_slice($asociados, 0, $cuantos);

            foreach ($pagadores as $asociadoId) {
                $this->crear(
                    ConceptoTransaccion::Mensualidad,
                    CarteraSeeder::MENSUALIDAD,
                    EstadoTransaccion::Aprobada,
                    MetodoPago::Pse,
                    ['asociado_id' => $asociadoId],
                    $mes->copy()->addDays(random_int(0, 26))->addHours(random_int(8, 20)),
                );
            }
        }
    }
```

Añadir el import que falte en la cabecera (`Asociado` ya está importado).

- [ ] **Step 4: Ejecutar hasta verde**

```bash
php artisan migrate:fresh --seed --no-interaction && php artisan test --compact --filter=SemillaConFormaTest
```

Esperado: PASS, 6 tests.

- [ ] **Step 5: Verificar que no se rompió el flujo de pagos**

```bash
php artisan test --compact --filter=FlujoDePagoTest
```

Esperado: PASS. Si falla por conteos de transacciones, ajustar el test para afirmar la regla y no el número exacto — **nunca** excluir el caso del barrido.

- [ ] **Step 6: Commit**

```bash
vendor/bin/pint --dirty --format agent
git add database/seeders/TransaccionSeeder.php tests/Feature/Panel/SemillaConFormaTest.php
git commit -m "Siembra 18 meses de mensualidades con estacionalidad

El tablero grafica recaudo mensual y todas las transacciones caian en
los ultimos 25 dias, asi que la grafica era una linea plana. Ahora la
serie tiene la forma del sector: diciembre factura, enero es el valle,
y la base de afiliados crece mes a mes."
```

---

# F3 — Tablero de mando

## Task 12: Servicio `ColaDePendientes`

**Files:**
- Create: `app/Panel/ColaDePendientes.php`
- Test: `tests/Feature/Panel/ColaDePendientesTest.php`

**Interfaces:**
- Consumes: `EsPublicable::scopePendiente()`, `Gate`, los nueve modelos publicables, `Filament::getModelResource()`.
- Produces:

```php
namespace App\Panel;

class ColaDePendientes
{
    /**
     * @return array<int, array{
     *     etiqueta: string,
     *     conteo: int,
     *     url: string,
     *     antiguedad: ?string,
     *     urgente: bool
     * }>
     */
    public function para(User $usuario): array

    public function total(User $usuario): int
}
```

**El principio que codifica:** se pregunta a la **policy**, no al rol. Es el mismo mecanismo de `FlujoDeAprobacionObserver::avisarAQuienAprueba()`, que consulta `$revisor->can('publicar', $modelo)`. Así la secretaría ve las tres bolsas, la dirección ve el contenido que redacta la secretaría, y si mañana cambia una policy la cola la sigue sin tocar código.

Se marca `urgente` cuando lo más antiguo pendiente lleva **más de 5 días** esperando.

- [ ] **Step 1: Escribir el test que falla**

```bash
php artisan make:test --phpunit Panel/ColaDePendientesTest
```

`tests/Feature/Panel/ColaDePendientesTest.php`:

```php
<?php

namespace Tests\Feature\Panel;

use App\Enums\EstadoPublicacion;
use App\Models\Asociado;
use App\Models\User;
use App\Models\Vacante;
use App\Panel\ColaDePendientes;
use Database\Seeders\RolYPermisoSeeder;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

/**
 * La cola de pendientes es lo primero que ve quien entra al panel.
 *
 * Pregunta a la policy y no al rol, igual que las notificaciones RF-37: así
 * la secretaría ve las bolsas, la dirección ve lo que redacta la secretaría,
 * y un cambio de policy mueve la cola sin tocar este servicio.
 */
class ColaDePendientesTest extends TestCase
{
    use RefreshDatabase;

    protected function setUp(): void
    {
        parent::setUp();
        $this->seed(RolYPermisoSeeder::class);
    }

    private function usuarioCon(string $rol): User
    {
        $usuario = User::factory()->create();
        $usuario->syncRoles([$rol]);

        return $usuario->fresh();
    }

    public function test_la_direccion_ve_pendiente_el_contenido_que_solo_ella_aprueba(): void
    {
        Asociado::factory()->create(['estado' => EstadoPublicacion::PendienteAprobacion]);

        $cola = app(ColaDePendientes::class)->para($this->usuarioCon(User::ROL_SUPER_ADMIN));

        $etiquetas = array_column($cola, 'etiqueta');
        $this->assertNotEmpty(
            array_filter($etiquetas, fn (string $e): bool => str_contains($e, 'asociado')),
            'La dirección aprueba asociados: debe verlos en su cola.'
        );
    }

    /**
     * La frontera negativa, que es la que prueba algo: la secretaría NO
     * aprueba asociados, así que un asociado pendiente no es su trabajo.
     */
    public function test_la_secretaria_no_ve_pendiente_lo_que_no_puede_aprobar(): void
    {
        Asociado::factory()->create(['estado' => EstadoPublicacion::PendienteAprobacion]);

        $cola = app(ColaDePendientes::class)->para($this->usuarioCon(User::ROL_SUBADMIN));

        $etiquetas = array_column($cola, 'etiqueta');
        $this->assertEmpty(
            array_filter($etiquetas, fn (string $e): bool => str_contains($e, 'asociado')),
            'La secretaría no publica asociados: no debe verlos como pendientes.'
        );
    }

    public function test_la_secretaria_si_ve_pendientes_las_bolsas(): void
    {
        $asociado = Asociado::factory()->create();
        Vacante::factory()->for($asociado)->create([
            'estado' => EstadoPublicacion::PendienteAprobacion,
        ]);

        $cola = app(ColaDePendientes::class)->para($this->usuarioCon(User::ROL_SUBADMIN));

        $etiquetas = array_column($cola, 'etiqueta');
        $this->assertNotEmpty(
            array_filter($etiquetas, fn (string $e): bool => str_contains($e, 'vacante')),
            'La secretaría aprueba las tres bolsas: la vacante es su trabajo.'
        );
    }

    public function test_la_cola_esta_vacia_cuando_no_hay_nada_pendiente(): void
    {
        Asociado::factory()->create(['estado' => EstadoPublicacion::Publicado]);

        $usuario = $this->usuarioCon(User::ROL_SUPER_ADMIN);

        $this->assertSame([], app(ColaDePendientes::class)->para($usuario));
        $this->assertSame(0, app(ColaDePendientes::class)->total($usuario));
    }

    public function test_cuenta_cuantos_hay_y_marca_urgente_lo_viejo(): void
    {
        Asociado::factory()->count(2)->create([
            'estado' => EstadoPublicacion::PendienteAprobacion,
            'updated_at' => now()->subDays(9),
        ]);

        $cola = app(ColaDePendientes::class)->para($this->usuarioCon(User::ROL_SUPER_ADMIN));

        $fila = collect($cola)->firstWhere(fn (array $f): bool => str_contains($f['etiqueta'], 'asociado'));

        $this->assertNotNull($fila);
        $this->assertSame(2, $fila['conteo']);
        $this->assertTrue($fila['urgente'], 'Nueve días esperando es urgente.');
        $this->assertNotNull($fila['antiguedad']);
    }

    public function test_el_total_suma_todas_las_filas(): void
    {
        $asociado = Asociado::factory()->create();
        Asociado::factory()->count(2)->create(['estado' => EstadoPublicacion::PendienteAprobacion]);
        Vacante::factory()->for($asociado)->create(['estado' => EstadoPublicacion::PendienteAprobacion]);

        $total = app(ColaDePendientes::class)->total($this->usuarioCon(User::ROL_SUPER_ADMIN));

        $this->assertSame(3, $total);
    }
}
```

- [ ] **Step 2: Ejecutar para verificar que falla**

```bash
php artisan test --compact --filter=ColaDePendientesTest
```

Esperado: FAIL con «Class "App\Panel\ColaDePendientes" not found».

- [ ] **Step 3: Crear el servicio**

```bash
php artisan make:class Panel/ColaDePendientes --no-interaction
```

`app/Panel/ColaDePendientes.php`:

```php
<?php

namespace App\Panel;

use App\Models\Aliado;
use App\Models\Artista;
use App\Models\Asociado;
use App\Models\Evento;
use App\Models\Iniciativa;
use App\Models\Noticia;
use App\Models\Proveedor;
use App\Models\RequisitoApertura;
use App\Models\User;
use App\Models\Vacante;
use Filament\Facades\Filament;
use Illuminate\Database\Eloquent\Model;
use Illuminate\Support\Facades\Gate;

/**
 * Qué tiene pendiente de aprobar quien acaba de entrar al panel.
 *
 * Se pregunta a la **policy** y no al rol, igual que hace
 * `FlujoDeAprobacionObserver::avisarAQuienAprueba()`. Así la secretaría ve
 * las tres bolsas —que sí aprueba—, la dirección ve el contenido que redacta
 * la secretaría, y el día que cambie una policy la cola la sigue sin que haya
 * que tocar nada aquí.
 */
class ColaDePendientes
{
    /** Los nueve modelos con flujo editorial, con su nombre en plural. */
    private const array PUBLICABLES = [
        Asociado::class => ['asociado', 'asociados'],
        Evento::class => ['evento', 'eventos'],
        Noticia::class => ['noticia', 'noticias'],
        RequisitoApertura::class => ['requisito', 'requisitos'],
        Iniciativa::class => ['iniciativa', 'iniciativas'],
        Vacante::class => ['vacante', 'vacantes'],
        Artista::class => ['artista', 'artistas'],
        Proveedor::class => ['proveedor', 'proveedores'],
        Aliado::class => ['aliado', 'aliados'],
    ];

    /** Días esperando a partir de los cuales un pendiente se marca urgente. */
    private const int DIAS_PARA_URGENTE = 5;

    /**
     * @return array<int, array{
     *     etiqueta: string,
     *     conteo: int,
     *     url: string,
     *     antiguedad: ?string,
     *     urgente: bool
     * }>
     */
    public function para(User $usuario): array
    {
        $filas = [];

        foreach (self::PUBLICABLES as $clase => [$singular, $plural]) {
            /** @var Model $modelo */
            $modelo = new $clase;

            // Instancia sin guardar: las policies de `publicar` resuelven por
            // permiso, no por el registro, así que basta para preguntar.
            if (! Gate::forUser($usuario)->allows('publicar', $modelo)) {
                continue;
            }

            $pendientes = $clase::query()->pendiente();
            $conteo = $pendientes->count();

            if ($conteo === 0) {
                continue;
            }

            $masAntigua = $clase::query()->pendiente()->min('updated_at');
            $espera = $masAntigua === null ? null : \Illuminate\Support\Carbon::parse($masAntigua);

            $filas[] = [
                'etiqueta' => $conteo === 1
                    ? "1 {$singular} esperando tu aprobación"
                    : "{$conteo} {$plural} esperando tu aprobación",
                'conteo' => $conteo,
                'url' => $this->urlDelListado($clase),
                'antiguedad' => $espera === null
                    ? null
                    : 'la más antigua, '.$espera->diffForHumans(),
                'urgente' => $espera !== null
                    && $espera->lt(now()->subDays(self::DIAS_PARA_URGENTE)),
            ];
        }

        // Lo más urgente primero, y a igual urgencia lo más numeroso.
        usort($filas, fn (array $a, array $b): int => [$b['urgente'], $b['conteo']] <=> [$a['urgente'], $a['conteo']]);

        return $filas;
    }

    public function total(User $usuario): int
    {
        return array_sum(array_column($this->para($usuario), 'conteo'));
    }

    /** @param  class-string<Model>  $clase */
    private function urlDelListado(string $clase): string
    {
        $recurso = Filament::getModelResource($clase);

        return $recurso === null ? url('/admin') : $recurso::getUrl('index');
    }
}
```

- [ ] **Step 4: Ejecutar hasta verde**

```bash
php artisan test --compact --filter=ColaDePendientesTest
```

Esperado: PASS, 6 tests.

- [ ] **Step 5: Commit**

```bash
vendor/bin/pint --dirty --format agent
git add app/Panel/ColaDePendientes.php tests/Feature/Panel/ColaDePendientesTest.php
git commit -m "Anade el servicio de cola de pendientes del panel

Lo pendiente de aprobar vivia solo en la campanita, y es el trabajo
diario de la secretaria. El servicio pregunta a la policy y no al rol,
igual que las notificaciones RF-37, asi que un cambio de policy mueve
la cola sin tocar este codigo."
```

---

## Task 13: Widget de la banda de acción

**Files:**
- Create: `app/Filament/Widgets/PendientesDeAprobacion.php`
- Create: `resources/views/filament/widgets/pendientes-de-aprobacion.blade.php`
- Test: `tests/Feature/Panel/TableroTest.php`

**Interfaces:**
- Consumes: `ColaDePendientes` (Tarea 12), `<x-panel.vidrio>` (Tarea 3), `<x-panel.cola>` (Tarea 5).
- Produces: widget `PendientesDeAprobacion` con `$sort = 0` y `$columnSpan = 'full'`, visible solo si el usuario tiene algo pendiente. Expone `public function filas(): array` (el resultado de `ColaDePendientes::para()`).

- [ ] **Step 1: Escribir el test que falla**

```bash
php artisan make:test --phpunit Panel/TableroTest
```

`tests/Feature/Panel/TableroTest.php`:

```php
<?php

namespace Tests\Feature\Panel;

use App\Enums\EstadoPublicacion;
use App\Filament\Widgets\PendientesDeAprobacion;
use App\Models\Asociado;
use App\Models\User;
use Database\Seeders\RolYPermisoSeeder;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Livewire\Livewire;
use Tests\TestCase;

/**
 * El tablero deja de ser un marcador y pasa a ser una cola de trabajo.
 */
class TableroTest extends TestCase
{
    use RefreshDatabase;

    protected function setUp(): void
    {
        parent::setUp();
        $this->seed(RolYPermisoSeeder::class);
    }

    private function usuarioCon(string $rol): User
    {
        $usuario = User::factory()->create();
        $usuario->syncRoles([$rol]);

        return $usuario->fresh();
    }

    public function test_la_banda_de_accion_lista_lo_pendiente_con_enlace(): void
    {
        Asociado::factory()->count(3)->create([
            'estado' => EstadoPublicacion::PendienteAprobacion,
        ]);

        $this->actingAs($this->usuarioCon(User::ROL_SUPER_ADMIN));

        Livewire::test(PendientesDeAprobacion::class)
            ->assertSee('3 asociados esperando tu aprobación')
            ->assertSee('Revisar');
    }

    public function test_la_banda_se_esconde_cuando_no_hay_nada_pendiente(): void
    {
        Asociado::factory()->create(['estado' => EstadoPublicacion::Publicado]);

        $this->actingAs($this->usuarioCon(User::ROL_SUPER_ADMIN));

        $this->assertFalse(PendientesDeAprobacion::canView());
    }

    /** Un tablero con una cola vacía enseña que el tablero no sirve. */
    public function test_la_banda_se_muestra_cuando_si_hay_pendientes(): void
    {
        Asociado::factory()->create(['estado' => EstadoPublicacion::PendienteAprobacion]);

        $this->actingAs($this->usuarioCon(User::ROL_SUPER_ADMIN));

        $this->assertTrue(PendientesDeAprobacion::canView());
    }
}
```

- [ ] **Step 2: Ejecutar para verificar que falla**

```bash
php artisan test --compact --filter=TableroTest
```

Esperado: FAIL con «Class "App\Filament\Widgets\PendientesDeAprobacion" not found».

- [ ] **Step 3: Crear el widget**

`app/Filament/Widgets/PendientesDeAprobacion.php`:

```php
<?php

namespace App\Filament\Widgets;

use App\Models\User;
use App\Panel\ColaDePendientes;
use Filament\Widgets\Widget;
use Illuminate\Support\Facades\Auth;

/**
 * Banda 1 del tablero: lo que hay que hacer hoy.
 *
 * Va arriba de todo y antes de cualquier cifra. Lo pendiente de aprobación
 * vivía solo en la campanita de notificaciones, y es el trabajo diario de la
 * secretaría: si no está a la vista, no existe.
 */
class PendientesDeAprobacion extends Widget
{
    protected string $view = 'filament.widgets.pendientes-de-aprobacion';

    protected static ?int $sort = 0;

    protected int|string|array $columnSpan = 'full';

    /**
     * @return array<int, array{
     *     etiqueta: string,
     *     conteo: int,
     *     url: string,
     *     antiguedad: ?string,
     *     urgente: bool
     * }>
     */
    public function filas(): array
    {
        $usuario = Auth::user();

        return $usuario instanceof User
            ? app(ColaDePendientes::class)->para($usuario)
            : [];
    }

    /**
     * Una cola vacía no se dibuja: un recuadro que dice «no hay nada» ocupa
     * el lugar más valioso del tablero para no decir nada.
     */
    public static function canView(): bool
    {
        $usuario = Auth::user();

        return $usuario instanceof User
            && app(ColaDePendientes::class)->total($usuario) > 0;
    }
}
```

- [ ] **Step 4: Crear la vista**

`resources/views/filament/widgets/pendientes-de-aprobacion.blade.php`:

```blade
<x-panel.vidrio resplandor class="p-5">
    <div class="flex items-center gap-2">
        <x-filament::icon icon="heroicon-o-inbox-arrow-down" class="h-5 w-5 text-acento" />
        <h2 class="font-display text-lg font-bold text-fuerte">Te está esperando</h2>
    </div>

    <p class="mt-1 text-sm text-tenue">
        Nadie aprueba lo que él mismo redactó: aquí solo aparece lo que te toca a ti.
    </p>

    <div class="mt-4">
        @foreach ($this->filas() as $fila)
            <x-panel.cola
                :etiqueta="$fila['etiqueta']"
                :url="$fila['url']"
                :antiguedad="$fila['antiguedad']"
                :urgente="$fila['urgente']"
            />
        @endforeach
    </div>
</x-panel.vidrio>
```

- [ ] **Step 5: Ejecutar hasta verde**

```bash
php artisan test --compact --filter=TableroTest
```

Esperado: PASS, 3 tests.

- [ ] **Step 6: Verificar la guardia de tema**

```bash
php artisan test --compact --filter=TemaClaroOscuroTest
```

Esperado: PASS — la vista nueva cae bajo `resources/views/filament`, que la Tarea 7 puso a vigilar.

- [ ] **Step 7: Commit**

```bash
vendor/bin/pint --dirty --format agent
git add app/Filament/Widgets/PendientesDeAprobacion.php resources/views/filament/widgets/pendientes-de-aprobacion.blade.php tests/Feature/Panel/TableroTest.php
git commit -m "Pone lo pendiente de aprobar arriba del tablero

Era el requisito critico RF-37 y vivia escondido en la campanita.
Ahora es la primera banda, con enlace directo al listado, y se esconde
cuando no hay nada: un recuadro que dice «no hay nada» ocupa el mejor
sitio del tablero para no decir nada."
```

---

## Task 14: KPIs de cuatro tarjetas por rol

**Files:**
- Modify: `app/Filament/Widgets/ResumenDelGremio.php`
- Delete: `app/Filament/Widgets/InscripcionesDelMes.php`
- Create: `app/Filament/Widgets/RecaudoMensual.php`
- Modify: `app/Providers/Filament/AdminPanelProvider.php`
- Modify: `tests/Feature/Panel/TableroTest.php`

**Interfaces:**
- Consumes: `<x-panel.kpi>` no aplica aquí (se usa `StatsOverviewWidget` de Filament, que ya trae su propia tarjeta y respeta el tema del panel); `Cartera::enMora()`, `Transaccion::aprobada()`, `Asociado::publicado()`, `Municipio`.
- Produces:
  - `ResumenDelGremio` con exactamente **4** stats, distintas según permisos, cada una con `->url(...)`.
  - `RecaudoMensual` (ChartWidget, `$sort = 2`, `columnSpan = 'full'`) con la serie del año en curso agregada **en SQL**.
  - `InscripcionesDelMes` eliminado.

**Los dos juegos de tarjetas:**

| Dirección (`ver_transaccion`) | Secretaría (sin `ver_transaccion`) |
|---|---|
| Recaudo del mes + delta vs. mes anterior | Pendientes de moderación |
| Cartera en mora + total | Bandeja sin responder |
| Asociados publicados + altas del mes | Postulaciones de la semana |
| Cobertura territorial (N de 12) | Fichas de bolsa por revisar |

`ver_transaccion` es el discriminador porque es exactamente el permiso que `RolYPermisoSeeder` le niega a la secretaría.

- [ ] **Step 1: Escribir los tests que fallan**

Añadir a `tests/Feature/Panel/TableroTest.php`:

```php
    public function test_la_direccion_ve_cuatro_tarjetas_con_la_plata(): void
    {
        \App\Models\Municipio::factory()->count(3)->create();

        $this->actingAs($this->usuarioCon(User::ROL_SUPER_ADMIN));

        Livewire::test(\App\Filament\Widgets\ResumenDelGremio::class)
            ->assertSee('Recaudado este mes')
            ->assertSee('Cartera en mora')
            ->assertSee('Cobertura territorial');
    }

    /** La secretaría no ve plata: no tiene `ver_transaccion` ni `ver_cartera`. */
    public function test_la_secretaria_ve_su_propio_juego_de_tarjetas(): void
    {
        $this->actingAs($this->usuarioCon(User::ROL_SUBADMIN));

        Livewire::test(\App\Filament\Widgets\ResumenDelGremio::class)
            ->assertSee('Pendientes de moderación')
            ->assertSee('Bandeja sin responder')
            ->assertDontSee('Recaudado este mes')
            ->assertDontSee('Cartera en mora');
    }

    public function test_el_resumen_muestra_exactamente_cuatro_tarjetas(): void
    {
        $this->actingAs($this->usuarioCon(User::ROL_SUPER_ADMIN));

        $widget = new \App\Filament\Widgets\ResumenDelGremio;
        $stats = (fn (): array => $this->getStats())->call($widget);

        $this->assertCount(4, $stats, 'Cuatro tarjetas, no seis: seis es un marcador, no un tablero.');
    }

    public function test_el_recaudo_mensual_reemplazo_a_las_inscripciones_de_30_dias(): void
    {
        $this->assertFalse(
            class_exists(\App\Filament\Widgets\InscripcionesDelMes::class),
            'La gráfica de 30 días era una línea plana en cero con eventos mensuales.'
        );
        $this->assertTrue(class_exists(\App\Filament\Widgets\RecaudoMensual::class));
    }

    /**
     * El widget anterior traía todos los modelos a memoria para agruparlos con
     * `groupBy` de Collection. Se mide el comportamiento —cuántas consultas
     * salen— y no cómo está escrito el archivo: una aserción sobre el texto
     * fuente se rompe con cualquier `->get()` legítimo en otro método.
     *
     * ⚠️ No hay `TransaccionFactory` en el proyecto: las transacciones se
     * crean a mano con la misma forma que usa `TransaccionSeeder`.
     */
    public function test_el_recaudo_mensual_agrega_en_una_sola_consulta(): void
    {
        foreach (range(1, (int) now()->format('n')) as $mes) {
            \App\Models\Transaccion::create([
                'referencia' => \App\Models\Transaccion::generarReferencia(),
                'concepto' => \App\Enums\ConceptoTransaccion::Mensualidad,
                'monto' => 50000,
                'moneda' => 'COP',
                'estado' => \App\Enums\EstadoTransaccion::Aprobada,
                'metodo' => \App\Enums\MetodoPago::Pse,
                'payload' => ['origen' => 'prueba'],
                'created_at' => now()->startOfYear()->addMonths($mes - 1)->addDay(),
            ]);
        }

        $consultas = [];
        \Illuminate\Support\Facades\DB::listen(function ($evento) use (&$consultas): void {
            $consultas[] = $evento->sql;
        });

        $widget = new \App\Filament\Widgets\RecaudoMensual;
        $datos = (fn (): array => $this->getData())->call($widget);

        $this->assertCount(
            1,
            $consultas,
            'La serie se agrega en SQL: una consulta, no una por mes ni una por fila.'
        );
        $this->assertStringContainsStringIgnoringCase('sum(monto)', $consultas[0]);
        $this->assertCount((int) now()->format('n'), $datos['labels']);
    }
```

- [ ] **Step 2: Ejecutar para verificar que falla**

```bash
php artisan test --compact --filter=TableroTest
```

Esperado: FAIL — `RecaudoMensual` no existe y `ResumenDelGremio` tiene 6 stats.

- [ ] **Step 3: Reescribir `ResumenDelGremio`**

Reemplazar el cuerpo de `app/Filament/Widgets/ResumenDelGremio.php` por:

```php
<?php

namespace App\Filament\Widgets;

use App\Enums\EstadoMensaje;
use App\Enums\EstadoPublicacion;
use App\Models\Artista;
use App\Models\Asociado;
use App\Models\Cartera;
use App\Models\Mensaje;
use App\Models\Municipio;
use App\Models\Postulacion;
use App\Models\Proveedor;
use App\Models\Transaccion;
use App\Models\User;
use App\Panel\ColaDePendientes;
use Filament\Widgets\StatsOverviewWidget;
use Filament\Widgets\StatsOverviewWidget\Stat;
use Illuminate\Support\Facades\Auth;

/**
 * Banda 2 del tablero: cuatro cifras, no seis, y distintas según el oficio.
 *
 * Antes eran seis tarjetas fijas que mezclaban contenido, plata, bandeja y
 * bolsa: la dirección y la secretaría abrían la misma pantalla con la misma
 * jerarquía y ninguna veía su trabajo. El discriminador es `ver_transaccion`,
 * que es justo el permiso que `RolYPermisoSeeder` le niega a la secretaría.
 *
 * Toda tarjeta lleva `url()`: un KPI que no es enlace es un número muerto.
 */
class ResumenDelGremio extends StatsOverviewWidget
{
    protected ?string $heading = 'El gremio hoy';

    protected static ?int $sort = 1;

    protected function getStats(): array
    {
        return Auth::user()?->can('ver_transaccion') === true
            ? $this->tarjetasDeDireccion()
            : $this->tarjetasDeSecretaria();
    }

    /** @return array<int, Stat> */
    private function tarjetasDeDireccion(): array
    {
        $inicioDeMes = now()->startOfMonth();
        $mesAnterior = now()->subMonthNoOverflow();

        $recaudado = $this->recaudadoEntre($inicioDeMes, now()->endOfMonth());
        $recaudadoAntes = $this->recaudadoEntre(
            $mesAnterior->copy()->startOfMonth(),
            $mesAnterior->copy()->endOfMonth()
        );

        $enMora = Cartera::enMora()->count();
        $saldo = (float) Cartera::enMora()->sum('saldo_pendiente');

        $publicados = Asociado::publicado()->count();
        $altasDelMes = Asociado::publicado()->where('created_at', '>=', $inicioDeMes)->count();

        $conPresencia = Municipio::whereHas('asociados', fn ($q) => $q->publicado())->count();

        return [
            Stat::make('Recaudado este mes', $this->pesos($recaudado))
                ->description($this->variacion($recaudado, $recaudadoAntes))
                ->descriptionIcon($recaudado >= $recaudadoAntes
                    ? 'heroicon-o-arrow-trending-up'
                    : 'heroicon-o-arrow-trending-down')
                ->color($recaudado >= $recaudadoAntes ? 'success' : 'danger')
                ->url(route('filament.admin.resources.transacciones.index')),

            Stat::make('Cartera en mora', $enMora)
                ->description($this->pesos($saldo).' por recaudar')
                ->descriptionIcon('heroicon-o-exclamation-triangle')
                ->color($enMora > 0 ? 'danger' : 'success')
                ->url(route('filament.admin.resources.cartera.index')),

            Stat::make('Asociados publicados', $publicados)
                ->description($altasDelMes.' altas este mes')
                ->descriptionIcon('heroicon-o-building-storefront')
                ->color('success')
                ->url(route('filament.admin.resources.asociados.index')),

            // Representatividad, que es el propósito #1 de la directiva: el
            // alcance firmado son los 12 municipios del Quindío.
            Stat::make('Cobertura territorial', $conPresencia.' de '.self::MUNICIPIOS_DEL_ALCANCE)
                ->description('municipios con presencia del gremio')
                ->descriptionIcon('heroicon-o-map-pin')
                ->color($conPresencia >= 8 ? 'success' : 'warning')
                ->url(route('filament.admin.resources.municipios.index')),
        ];
    }

    /** @return array<int, Stat> */
    private function tarjetasDeSecretaria(): array
    {
        $usuario = Auth::user();
        $pendientes = $usuario instanceof User
            ? app(ColaDePendientes::class)->total($usuario)
            : 0;

        $sinResponder = Mensaje::where('estado', EstadoMensaje::Nuevo)->count();
        $pqrAbiertos = Mensaje::whereNotNull('radicado')
            ->where('estado', '!=', EstadoMensaje::Respondido)
            ->count();

        $postulaciones = Postulacion::where('created_at', '>=', now()->subWeek())->count();

        $fichas = Artista::query()->pendiente()->count()
            + Proveedor::query()->pendiente()->count();

        return [
            Stat::make('Pendientes de moderación', $pendientes)
                ->description('esperan tu aprobación')
                ->descriptionIcon('heroicon-o-clock')
                ->color($pendientes > 0 ? 'warning' : 'success')
                ->url(route('filament.admin.resources.vacantes.index')),

            Stat::make('Bandeja sin responder', $sinResponder)
                ->description($pqrAbiertos.' PQR abiertos')
                ->descriptionIcon('heroicon-o-inbox')
                ->color($pqrAbiertos > 0 ? 'danger' : 'warning')
                ->url(route('filament.admin.resources.mensajes.index')),

            Stat::make('Postulaciones de la semana', $postulaciones)
                ->description('candidatos que llegaron por la bolsa')
                ->descriptionIcon('heroicon-o-user-plus')
                ->color('info')
                ->url(route('filament.admin.resources.postulaciones.index')),

            Stat::make('Fichas de bolsa por revisar', $fichas)
                ->description('artistas y proveedores inscritos')
                ->descriptionIcon('heroicon-o-identification')
                ->color($fichas > 0 ? 'warning' : 'success')
                ->url(route('filament.admin.resources.artistas.index')),
        ];
    }

    /** El alcance firmado del proyecto son los 12 municipios del Quindío. */
    private const int MUNICIPIOS_DEL_ALCANCE = 12;

    private function recaudadoEntre(\DateTimeInterface $desde, \DateTimeInterface $hasta): float
    {
        return (float) Transaccion::aprobada()
            ->whereBetween('created_at', [$desde, $hasta])
            ->sum('monto');
    }

    private function variacion(float $ahora, float $antes): string
    {
        if ($antes <= 0.0) {
            return $ahora > 0.0 ? 'primer mes con recaudo' : 'sin recaudo el mes pasado';
        }

        $porcentaje = (($ahora - $antes) / $antes) * 100;
        $signo = $porcentaje >= 0 ? '+' : '−';

        return $signo.number_format(abs($porcentaje), 1, ',', '.').' % vs. mes anterior';
    }

    private function pesos(float $monto): string
    {
        return '$'.number_format($monto, 0, ',', '.');
    }

    public static function canView(): bool
    {
        return Auth::user()?->can('ver_asociado') === true;
    }
}
```

> **Nombres de ruta, ya verificados contra `php artisan route:list --path=admin --name=resources`.** Cuidado: **no** se derivan del nombre de la carpeta del recurso. Aunque las carpetas se llamen `Transaccions`, `Carteras`, `Proveedors` e `Inscripcions`, las rutas usan plurales españoles correctos porque los recursos declaran su propio slug. Los seis que usa este widget son, exactamente:
>
> - `filament.admin.resources.transacciones.index`
> - `filament.admin.resources.cartera.index` — **en singular**
> - `filament.admin.resources.asociados.index`
> - `filament.admin.resources.municipios.index`
> - `filament.admin.resources.mensajes.index`
> - `filament.admin.resources.postulaciones.index`
> - `filament.admin.resources.vacantes.index`
> - `filament.admin.resources.artistas.index`

- [ ] **Step 4: Crear `RecaudoMensual` y eliminar `InscripcionesDelMes`**

```bash
git rm app/Filament/Widgets/InscripcionesDelMes.php
```

`app/Filament/Widgets/RecaudoMensual.php`:

```php
<?php

namespace App\Filament\Widgets;

use App\Models\Transaccion;
use Filament\Widgets\ChartWidget;
use Illuminate\Support\Facades\Auth;

/**
 * Banda 3 del tablero: recaudo del año en curso, mes a mes.
 *
 * Reemplaza a «Inscripciones de los últimos 30 días», que con eventos
 * mensuales era una línea plana en cero unos 28 días de cada 30 — y una
 * gráfica vacía no es neutra, enseña que el tablero no sirve.
 *
 * Horizonte deliberadamente corto y operativo («¿vamos bien este mes?»); la
 * serie larga de 18 meses vive en el Observatorio, con propósito analítico.
 *
 * La agregación va en SQL: el widget anterior traía todos los modelos a
 * memoria para contarlos con `groupBy` de Collection.
 */
class RecaudoMensual extends ChartWidget
{
    protected ?string $heading = 'Recaudo mes a mes, este año';

    protected static ?int $sort = 2;

    protected int|string|array $columnSpan = 'full';

    private const array MESES = [
        1 => 'Ene', 2 => 'Feb', 3 => 'Mar', 4 => 'Abr', 5 => 'May', 6 => 'Jun',
        7 => 'Jul', 8 => 'Ago', 9 => 'Sep', 10 => 'Oct', 11 => 'Nov', 12 => 'Dic',
    ];

    protected function getData(): array
    {
        // `strftime` es de SQLite. Se aísla aquí y no en un scope para que el
        // día que el motor cambie a PostgreSQL solo haya que tocar este mapa.
        $expresion = match (\Illuminate\Support\Facades\DB::connection()->getDriverName()) {
            'pgsql' => "to_char(created_at, 'MM')",
            'mysql', 'mariadb' => "date_format(created_at, '%m')",
            default => "strftime('%m', created_at)",
        };

        $porMes = Transaccion::aprobada()
            ->whereBetween('created_at', [now()->startOfYear(), now()->endOfYear()])
            ->selectRaw("{$expresion} as mes, sum(monto) as total")
            ->groupBy('mes')
            ->pluck('total', 'mes');

        $etiquetas = [];
        $valores = [];

        foreach (range(1, (int) now()->format('n')) as $mes) {
            $etiquetas[] = self::MESES[$mes];
            $valores[] = (float) ($porMes[str_pad((string) $mes, 2, '0', STR_PAD_LEFT)] ?? 0);
        }

        return [
            'datasets' => [[
                'label' => 'Recaudado (COP)',
                'data' => $valores,
                'fill' => true,
                'tension' => 0.3,
            ]],
            'labels' => $etiquetas,
        ];
    }

    protected function getType(): string
    {
        return 'line';
    }

    protected function getOptions(): array
    {
        return [
            'plugins' => ['legend' => ['display' => false]],
            'scales' => [
                'y' => ['beginAtZero' => true, 'ticks' => [], 'grid' => []],
                'x' => ['ticks' => [], 'grid' => ['display' => false]],
            ],
        ];
    }

    public static function canView(): bool
    {
        return Auth::user()?->can('ver_transaccion') === true;
    }
}
```

- [ ] **Step 5: Actualizar el provider**

En `AdminPanelProvider`, cambiar el import y el array `widgets()`:

```php
use App\Filament\Widgets\AsociadosPorMunicipio;
use App\Filament\Widgets\PendientesDeAprobacion;
use App\Filament\Widgets\RecaudoMensual;
use App\Filament\Widgets\ResumenDelGremio;
use App\Filament\Widgets\UltimasTransacciones;
```

```php
->widgets([
    PendientesDeAprobacion::class,
    ResumenDelGremio::class,
    RecaudoMensual::class,
    AsociadosPorMunicipio::class,
    UltimasTransacciones::class,
])
```

- [ ] **Step 6: Ejecutar hasta verde**

```bash
php artisan test --compact --filter=TableroTest
```

Esperado: PASS, 8 tests. Si alguno falla con `Route [...] not defined`, contrastar con `php artisan route:list --path=admin --name=resources`.

- [ ] **Step 7: Verificar que no se rompió el panel**

```bash
php artisan test --compact --filter=PanelAdminTest
php artisan test --compact --filter=PanelCompletoTest
```

Esperado: PASS en ambos.

- [ ] **Step 8: Commit**

```bash
vendor/bin/pint --dirty --format agent
git add app/Filament/Widgets app/Providers/Filament/AdminPanelProvider.php tests/Feature/Panel/TableroTest.php
git commit -m "Da a cada rol su propio juego de cuatro KPIs enlazados

Eran seis tarjetas fijas que mezclaban contenido, plata, bandeja y
bolsa: la direccion y la secretaria abrian la misma pantalla y ninguna
veia su trabajo. Ahora son cuatro por oficio y todas llevan a su
listado. Y la grafica de inscripciones a 30 dias, que con eventos
mensuales era una linea plana en cero, deja su sitio al recaudo mensual
agregado en SQL."
```

---

## Task 15: Página propia del tablero

**Files:**
- Create: `app/Filament/Pages/Dashboard.php`
- Modify: `app/Providers/Filament/AdminPanelProvider.php`
- Modify: `tests/Feature/Panel/TableroTest.php`

**Interfaces:**
- Consumes: todos los widgets de las Tareas 13–14.
- Produces: `App\Filament\Pages\Dashboard` extendiendo `Filament\Pages\Dashboard`, con título «Tablero del gremio», subtítulo, y rejilla responsive de 4 columnas en `md` y 4 en `xl`. El `Dashboard::class` de fábrica sale de `pages()`.

- [ ] **Step 1: Escribir el test que falla**

Añadir a `tests/Feature/Panel/TableroTest.php`:

```php
    public function test_el_tablero_es_una_pagina_propia_con_titulo_del_gremio(): void
    {
        $this->actingAs($this->usuarioCon(User::ROL_SUPER_ADMIN));

        $this->get('/admin')
            ->assertOk()
            ->assertSee('Tablero del gremio');
    }

    public function test_el_tablero_de_fabrica_ya_no_esta_registrado(): void
    {
        $panel = (new \App\Providers\Filament\AdminPanelProvider($this->app))
            ->panel(\Filament\Panel::make());

        $paginas = $panel->getPages();

        $this->assertNotContains(\Filament\Pages\Dashboard::class, $paginas);
    }

    public function test_la_secretaria_tambien_entra_al_tablero(): void
    {
        $this->actingAs($this->usuarioCon(User::ROL_SUBADMIN));

        $this->get('/admin')->assertOk()->assertSee('Tablero del gremio');
    }
```

- [ ] **Step 2: Ejecutar para verificar que falla**

```bash
php artisan test --compact --filter=TableroTest
```

Esperado: FAIL — «Tablero del gremio» no aparece.

- [ ] **Step 3: Crear la página**

`app/Filament/Pages/Dashboard.php`:

```php
<?php

namespace App\Filament\Pages;

use Filament\Pages\Dashboard as TableroDeFabrica;

/**
 * El tablero del panel, con vocabulario del gremio y rejilla propia.
 *
 * Reemplaza al de fábrica para poder ordenar las tres bandas del diseño:
 * primero lo que hay que hacer (`PendientesDeAprobacion`), después las cuatro
 * cifras del oficio (`ResumenDelGremio`), y al final las gráficas.
 */
class Dashboard extends TableroDeFabrica
{
    protected static ?string $title = 'Tablero del gremio';

    protected static ?string $navigationLabel = 'Tablero';

    protected static ?string $navigationIcon = 'heroicon-o-home';

    public function getSubheading(): ?string
    {
        return 'Lo que te espera hoy, y cómo va el gremio.';
    }

    /** @return int|array<string, int> */
    public function getColumns(): int|array
    {
        // Cuatro columnas: es lo que necesitan las cuatro tarjetas de la
        // banda 2 para caber en una sola fila en escritorio.
        return [
            'default' => 1,
            'sm' => 2,
            'xl' => 4,
        ];
    }
}
```

- [ ] **Step 4: Quitar el tablero de fábrica del provider**

En `AdminPanelProvider`, eliminar el import `use Filament\Pages\Dashboard;` y dejar `pages()` vacío — `discoverPages()` encuentra el nuevo:

```php
->discoverPages(in: app_path('Filament/Pages'), for: 'App\Filament\Pages')
->pages([])
```

- [ ] **Step 5: Ejecutar hasta verde**

```bash
php artisan test --compact --filter=TableroTest
```

Esperado: PASS, 11 tests.

- [ ] **Step 6: Verificación completa de F1–F3**

```bash
php artisan migrate:fresh --seed --no-interaction
php artisan view:clear && npm run build
php artisan test --compact
```

Esperado: build sin errores y **toda la suite verde**. El total debe haber subido desde 374 (los tests nuevos de las Tareas 2–15). Si algo rojo aparece en tests preexistentes, arreglarlo **afirmando la regla nueva**, nunca excluyendo el caso del barrido.

- [ ] **Step 7: Revisar a ojo los dos temas**

Levantar el panel, entrar como `direccion@asobaresquindio.test` / `Asobares2026*` y luego como `oficina@asobaresquindio.test`. En cada uno, alternar claro/oscuro desde el menú de Filament y comprobar:

- La banda «Te está esperando» se ve con vidrio y resplandor en oscuro, y con sombra y borde en claro.
- Las cuatro tarjetas son distintas entre los dos usuarios.
- Las etiquetas de los ejes de las dos gráficas cambian de color con el tema.
- Ninguna cifra queda ilegible en ninguno de los dos temas.

- [ ] **Step 8: Commit**

```bash
vendor/bin/pint --dirty --format agent
git add app/Filament/Pages/Dashboard.php app/Providers/Filament/AdminPanelProvider.php tests/Feature/Panel/TableroTest.php
git commit -m "Reemplaza el tablero de fabrica por el del gremio

Hacia falta una pagina propia para poder ordenar las tres bandas del
diseno: primero lo que hay que hacer, despues las cifras del oficio, y
al final las graficas. Con la rejilla de cuatro columnas las tarjetas
caben en una sola fila en escritorio."
```

---

## Autorrevisión del plan

**Cobertura del spec (F1–F3):**

| Requisito del spec | Tarea |
|---|---|
| §1 tokens compartidos en `tokens.css` | 1 |
| §1 tema de Filament + Poppins | 2 |
| §1 tokens de vidrio con dos recetas | 3 |
| §1 mordaza de transiciones al cambiar tema | **Ver nota abajo** |
| §1 muere el `#EE4137` cableado | 6 |
| §2 `<x-panel.vidrio>` | 3 |
| §2 `<x-panel.kpi>` | 4 |
| §2 `<x-panel.cola>` | 5 |
| §2 `<x-panel.grafica>` | 6 (como plugin global, no componente Blade — ver nota) |
| §2 movimiento bajo `prefers-reduced-motion` | 3, 4 |
| §3 banda 1: cola accionable | 12, 13 |
| §3 banda 2: 4 KPIs por rol | 14 |
| §3 banda 3: gráfica con serie real, agregada en SQL | 14 |
| §4 tabla `consultas_guia` sin datos personales | 8, 9 |
| §6 semilla con forma | 10, 11 |
| §9 guardia de tokens extendida al panel | 7 |
| §9 KPIs por rol probados | 14 |
| §9 cola pregunta a la policy, con frontera negativa | 12 |
| §10 orden F1 → F2 → F3 | Tareas 1–7 → 8–11 → 12–15 |

**Dos desviaciones conscientes, ambas señaladas en su sitio:**

1. **`<x-panel.grafica>` no es un componente Blade.** Filament renderiza sus `ChartWidget` con su propia vista y su propio Alpine; envolverlos en un componente Blade obligaría a sobrescribir vistas de Filament, que es justo el riesgo que el enfoque híbrido evita. El mismo resultado —gráficas que siguen el tema— se consigue con un plugin global de Chart.js (Tarea 6), que además cubre **todos** los widgets presentes y futuros sin tocarlos uno por uno. Es mejor solución que la del spec.

2. **La mordaza de transiciones del cambio de tema no se replica en el panel.** El sitio la necesita porque tiene `transition-colors` repartido por todas las vistas. En el panel, el único movimiento que introducimos es `.vidrio-hover` (que transiciona `border-color` y `transform`), y no está en un elemento cuyo color dependa del tema en el instante del cambio. Añadir la mordaza sin necesidad es código que hay que mantener sin razón. **Si en la revisión visual del Paso 7 de la Tarea 15 aparece un color congelado, hay que añadirla** — queda anotado como el primer sitio donde mirar.

**Consistencia de tipos:** `ColaDePendientes::para()` devuelve el array shape `{etiqueta, conteo, url, antiguedad, urgente}`; lo consumen `PendientesDeAprobacion::filas()` (Tarea 13) y la vista, que pasa `etiqueta`, `url`, `antiguedad` y `urgente` a `<x-panel.cola>` — cuya firma (Tarea 5) acepta exactamente esos cuatro más `icono` y `accion`, ambos con valor por omisión. `total()` lo consume `canView()` (Tarea 13) y `tarjetasDeSecretaria()` (Tarea 14). Coincide.

**Supuestos verificados contra el código antes de dar el plan por bueno:**

- Los **nombres de ruta** de los ocho recursos que enlaza la Tarea 14 salen de `php artisan route:list --path=admin --name=resources`, no de una deducción. La trampa que había: las carpetas se llaman `Transaccions` y `Carteras`, pero las rutas son `transacciones` y **`cartera` en singular**.
- Los **nueve modelos publicables** de `ColaDePendientes::PUBLICABLES` son exactamente los nueve que usan el trait `EsPublicable` (`Aliado`, `Artista`, `Asociado`, `Evento`, `Iniciativa`, `Noticia`, `Proveedor`, `RequisitoApertura`, `Vacante`), y coinciden con `RolYPermisoSeeder::PUBLICABLES`.
- `Municipio::asociados()` y `Municipio::requisitos()` existen (`app/Models/Municipio.php:29` y `:35`), que es lo que necesitan la cobertura territorial y el registro de consultas.
- `Postulacion`, `Aliado`, `Iniciativa`, `Artista` y `Proveedor` existen como modelos.

**Riesgo abierto real:** que `make:filament-theme` de Filament 4.12 genere una cabecera distinta de la que asume la Tarea 2. Por eso el Paso 2 dice *conservar la primera línea que genere el comando* y añadir debajo, en vez de dictar el archivo entero.
