{{-- El campo de puntos del fondo de toda la interfaz del panel. Decoración
     pura: sin texto, sin puntero y por debajo de los módulos, así que no entra
     en ninguna cuenta de contraste. Lo dibuja `panel-barra-puntos.js`. --}}
<canvas class="asb-barra-puntos" aria-hidden="true"></canvas>
