{{-- El campo de puntos del fondo de la barra (D-L24). Decoración pura: sin
     texto, sin puntero y por debajo de los módulos, así que no entra en
     ninguna cuenta de contraste. Lo dibuja `panel-barra-puntos.js`. --}}
<canvas class="asb-barra-puntos" aria-hidden="true"></canvas>
